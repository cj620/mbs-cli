# 发布 1.0.4 维护版本执行计划

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 发布 1.0.4 维护版本 |
| 任务级别 | L3（公共请求契约、远程分支与生产 npm 发布） |
| 任务类型 | RELEASE |
| 任务状态 | 执行中 |
| 当前阶段 | 发布前 V3 完成，待精确提交 |
| 交付状态 | 未发布 |
| 执行授权 | 已授权 |
| 授权来源 | 用户明确要求“提交代码并发布1.0.4版本” |
| 授权范围 | 升级 1.0.4、精确提交并普通推送 `origin/1.0.0`、创建并推送 `maintenance-1-v1.0.4`、通过既有工作流发布 npm `maintenance-1`；不含 master、latest、强推、删除引用、覆盖旧版本或真实业务请求 |
| 计划完成后动作 | 当前范围内继续实施直至发布后核验与审计回填完成 |
| 验证级别 | V3 |
| 创建和更新时间 | 2026-08-25 |
| 需求文档 | [`./需求描述.md`](./需求描述.md) |
| 仓库与分支 | `D:\workspace\mbs-cli` / `1.0.0`，上游 `origin/1.0.0` |
| 基线 | `5a8f4fd76d0cd7a62e95c4b43c5ffa3029d06a9f` |
| 执行环境 | Windows / PowerShell / Node 20 兼容项目工具链 / pnpm 10.28.2 |

## 恢复执行入口

- **任务目标**：把接口请求体编码端到端支持作为 `1.0.4` 发布到 npm `maintenance-1`，不移动普通 `latest`。
- **当前进度**：CLI 已升级为 `1.0.4`；frozen install、manifest dry-run、全工作区构建、191 条测试、13 条发布保护、代表性命令、编译产物、差异与敏感检查均通过。
- **下一步**：复核 staged diff，创建发布提交并普通推送 `origin/1.0.0`，等待对应分支 CI 成功。
- **阻塞**：无。
- **待确认**：无；用户已明确指定版本、提交和发布。
- **未验证**：GitHub 分支 CI、Release、npm 官方包和目标真实业务接口。
- **重要警告**：只有维护分支 CI 成功后才能创建标签；不得移动 `latest=0.1.59`；`.idea/claudeCodeTabState.xml` 必须保留本地且不提交。
- **执行授权**：已授权。
- **当前允许操作**：发布范围文档/版本修改、本地验证、精确暂存提交、普通推送维护分支、创建并推送维护标签、读取 GitHub Actions 与 npm 官方状态、隔离安装核验。
- **当前禁止操作**：修改/推送 master、移动 latest、强推、删除或覆盖引用、覆盖已发布 npm 版本、真实业务请求、读取或记录敏感信息。
- **授权后的第一步**：已核对工作区、需求、计划、基线、上游分支、现有修改和版本占用。

## 已确认决策、执行假设与约束

| 编号 | 类型 | 内容 | 依据 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| D-001 | 决策 | 使用 `1.0.4` 与 `maintenance-1-v1.0.4` | 用户指定；Git 标签和 npm 精确版本均未占用 | 有效 |
| D-002 | 决策 | 只发布到 `maintenance-1` | `1.0.0` 长期维护线既有契约 | 有效 |
| D-003 | 决策 | 发布前 `latest=0.1.59`，发布后必须保持不变 | npm 官方源发布前核验 | 有效 |
| D-004 | 决策 | 标签只指向通过 CI 的维护分支提交 | Release 工作流来源分支门禁 | 有效 |
| D-005 | 决策 | `.idea/claudeCodeTabState.xml` 不进入任何提交 | 功能任务已有隔离记录，属于用户本地修改 | 有效 |
| A-001 | 假设 | 真实目标网关请求不作为本次发布门禁 | 当前功能任务已完成本地 V3；本次不具备安全的生产请求样本，发布后仍披露该未验证项 | 待发布后复核 |

## 影响范围

- **预计修改**：`packages/cli/package.json`、接口请求体编码功能及其既有工作区差异、相关测试/Skill/README/记忆/审计、本发布任务文档与索引。
- **明确不修改**：`.idea/claudeCodeTabState.xml` 内容、master、latest、认证配置、生产数据与外部业务系统。
- **关联参考**：[`../20260825-[FEATURE]接口请求体编码端到端支持/`](../20260825-[FEATURE]接口请求体编码端到端支持/)、[`../../memory/topics/1.0.0长期维护分支.md`](../../memory/topics/1.0.0长期维护分支.md)。

## 执行前提

- `origin/1.0.0` 必须仍指向基线或当前提交的祖先，禁止非快进覆盖。
- npm `1.0.4` 和远端 `maintenance-1-v1.0.4` 必须未占用。
- GitHub 连接具备仓库读取和推送权限，仓库 Release 工作流具备 npm 发布密钥。
- 本地验证不得读取真实登录态或发送业务请求；临时打包/安装目录必须可清理。

## 执行步骤

### S1：冻结发布范围与远端状态

- **状态**：已完成
- **目标**：确认分支、基线、现有修改归属、版本/标签占用和发布通道。
- **实际结果**：本地与远端 `1.0.0` 均为 `5a8f4fd`；`1.0.4` 与维护标签未占用；npm 为 `maintenance-1=1.0.3`、`latest=0.1.59`；功能差异与任务文档一致，`.idea` 明确排除。
- **验证证据**：`git status`、`git ls-remote`、npm 官方源查询、GitHub 仓库元数据。

### S2：升级版本并执行本地 V3

- **状态**：已完成
- **目标**：在任何远程写入前证明版本、功能、构建、测试和发布保护满足门禁。
- **操作**：升级 CLI 版本；排除 `.idea`；执行 frozen install、manifest dry-run、构建、全量测试、发布脚本/工作流/bundle 保护、代表性命令、差异和敏感信息检查。
- **通过标准**：全部核心检查通过，暂存差异只含发布范围，未发现凭据或未说明失败。
- **失败处理**：停止远程写入；记录失败并在范围内修复或将任务标记已阻塞。
- **实际结果**：`pnpm install --frozen-lockfile`、manifest dry-run、14 个工作区构建、根脚本与工作区合计 191 条测试全部通过；发布工作流 1 项、发布脚本 7 条、bundle 5 条合计 13 条保护通过；CLI 显示 `1.0.4`，request/serve 帮助、global/pms Skill、编译产物模式与 `--api-id` 均核验通过；敏感模式和新增禁止写法均为 0，`.idea` 未暂存。
- **验证证据**：manifest hash `2b24bbad...41d84df`；shared 67 条、doris 21 条、export 3 条、legacy 10 条、CLI 77 条、根脚本 13 条；`git diff --check` 仅报告既有 LF/CRLF 转换提示且退出 0。

### S3：精确提交并推送维护分支

- **状态**：未开始
- **目标**：创建可追溯发布提交并通过维护分支 CI。
- **操作**：逐路径暂存和复核 staged diff；提交；普通推送 `HEAD:1.0.0`；观察对应 GitHub Actions CI。
- **通过标准**：用户 `.idea` 修改未提交；远端提交与本地一致；CI 成功。
- **失败处理**：不强推、不覆盖远端；保留本地提交，调查后追加修复提交。

### S4：创建维护标签并发布 npm

- **状态**：未开始
- **前置条件**：S3 的维护分支 CI 成功。
- **操作**：创建轻量标签 `maintenance-1-v1.0.4` 并普通推送；观察 Release 工作流。
- **通过标准**：标签指向通过 CI 的提交；Release 成功；npm 精确版本与 dist-tag 正确，latest 未移动。
- **失败处理**：不移动或覆盖标签；保留失败证据，使用普通追加提交和新的未发布版本恢复。

### S5：核验官方源产物并回填审计

- **状态**：未开始
- **操作**：从 npm 官方源隔离安装精确版本；核验版本、request/Skill 入口和编译产物；更新功能任务、记忆、索引和本发布记录并提交推送纯文档回填。
- **通过标准**：官方包核验通过；任务状态与实际发布证据一致；本地只保留排除的 `.idea` 修改。
- **失败处理**：任务保持已阻塞或待验收，明确 npm 已发布但产物核验失败；不得覆盖 1.0.4。

## 验收检查

| 编号 | 验收项 | 验证级别 | 预期 | 当前状态 |
| :--- | :--- | :--- | :--- | :--- |
| AC-01 | 本地 V3 | V3 | frozen install、生成、构建、191 条测试、13 条发布保护和代表性命令通过 | 通过 |
| AC-02 | 提交范围 | V3 | 只含接口请求体功能、版本与审计；`.idea` 排除 | 通过 |
| AC-03 | 维护分支门禁 | V3 | 发布提交已推送且 CI 成功 | 待执行 |
| AC-04 | 维护发布 | V3 | 标签与 Release 成功，npm 存在 1.0.4 | 待执行 |
| AC-05 | 通道隔离 | V3 | `maintenance-1=1.0.4`，`latest=0.1.59` | 待执行 |
| AC-06 | 官方源产物 | V3 | 隔离安装版本、公开入口与请求体编码能力核验通过 | 待执行 |

## 风险、数据保护与回滚

| 风险 | 控制 | 回滚或失败处置 |
| :--- | :--- | :--- |
| 非 JSON 编码与目标后端契约不一致 | 本地七模式测试、详情校验、旧 JSON 兼容；真实业务请求保持未验证披露 | npm 版本不可覆盖；停止扩大发布，后续以新补丁版本修复 |
| 意外影响普通安装用户 | 标签前后核对 npm dist-tags，只使用维护标签 | 立即停止，不通过移动 latest 补救 |
| 文件输入泄露或越界 | 有界普通文件、失败关闭、日志/文档敏感检查；不发送真实文件 | 停止发布并修复，清理受控临时文件 |
| 混入用户 IDE 状态 | 撤出 `.idea` 暂存并逐路径复核 staged diff | 提交前停止；不覆盖用户文件 |
| 分支或工作流失败 | 本地 V3、分支 CI、Release 三道门禁 | 保留证据，普通追加提交；禁止强推和复用已发布版本 |

## Git、依赖、安全与规则记录

- 仓库：`D:\workspace\mbs-cli`；分支：`1.0.0`；基线：`5a8f4fd`；远端目标：`origin/1.0.0`。
- 计划 Git 操作：精确暂存、提交、普通推送维护分支；CI 成功后创建并推送一个新轻量标签；不创建 PR，不操作 master。
- 本次不新增或升级依赖；使用现有 `pnpm-lock.yaml` 与项目固定 pnpm 版本。
- 敏感信息：不读取登录态、环境变量值、Cookie、真实请求/响应或上传内容；扫描只输出文件或检查结论。
- 已完整读取 R-01、R-02、R-03、R-04、R-05、R-06、R-07、R-08、R-09、R-10，触发原因为 L3 发布、代码/输入/资源、安全、验证、Git/版本、技能和项目记忆。
- 已使用 GitHub 技能；本机无 `gh` CLI，远端读取改用 GitHub 连接器和官方 Git/npm 接口。

## 任务外发现

- 发布前 npm 普通通道已从历史记录的 `latest=0.1.58` 演进为 `0.1.59`；本次只冻结并保持当前值，不追溯该普通通道发布。

## 变更记录

| 时间 | 变更 | 原因 |
| :--- | :--- | :--- |
| 2026-08-25 | 创建发布需求和计划，选择 1.0.4 | 用户授权提交发布；确认指定版本与标签未占用 |
| 2026-08-25 | 将 latest 保持值更新为 0.1.59 | npm 官方源发布前事实优先于历史 1.0.3 记录 |
| 2026-08-25 | 完成版本升级与本地 V3 | 在远程写入前完成 frozen install、生成、构建、191 条测试、13 条发布保护和发布范围检查 |
