# 恢复已发布 CLI 旧命令执行计划

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 恢复已发布 CLI 旧命令 |
| 任务级别 | L3（公开 CLI 契约、跨插件兼容） |
| 任务类型 | BUG |
| 任务状态 | 待验收 |
| 当前阶段 | 本地验证完成，等待目标环境验收 |
| 交付状态 | 待发布 |
| 执行授权 | 已授权 |
| 授权来源 | 用户明确要求“先恢复五个旧命令” |
| 授权范围 | 本地恢复五个旧 CLI 命令、插件注册、必要测试和任务文档；不含后端、serve 旧路由、Git 提交推送和发布 |
| 计划完成后动作 | 当前范围内继续实施并交付本地验证结果 |
| 验证级别 | V3 |
| 创建时间 | 2026-08-10 |
| 最后更新时间 | 2026-08-10 |
| 需求文档 | [`./需求描述.md`](./需求描述.md) |
| 仓库与分支 | `D:\workspace\mbs-cli` / `20260722-FEATURE-接口检索降噪` |
| 基线 | `5b5c255c9d6b9e338d8d2717cf17f2c27a2d2c3d` |
| 执行环境 | Windows / PowerShell / Node 20 项目基线 / pnpm workspace |

## 恢复执行入口

- **任务目标**：以独立兼容插件恢复 npm `0.1.58` 的五个旧命令契约。
- **当前进度**：独立兼容插件、五个旧命令、冻结契约测试、workspace 注册及本地 V3 验证均已完成。
- **下一步**：在生产等价网关执行五个脱敏只读最小查询；通过后进入灰度发布流程。
- **阻塞**：无。
- **待确认**：无；旧 `serve` 路由明确不在本轮范围。
- **未验证**：真实目标网关上五个旧接口的可达性、安装包发布产物和用户灰度。
- **重要警告**：工作区开始时已有 1610 项修改，其中 1552 项为删除；不得运行会重写全量生成产物的 manifest 生成命令，不得覆盖这些既有修改。
- **执行授权**：已授权。
- **当前允许操作**：读取、在任务范围新增兼容插件和测试、修改 CLI 插件注册、更新锁文件、执行本地非生产验证。
- **当前禁止操作**：修改后端、调用生产业务接口、恢复 serve 旧路由、删除现有文件、暂存、提交、推送、发布和生产写入。
- **授权方式**：用户已在当前会话明确授权。
- **授权后的第一步**：已核对工作区、需求、计划、基线和已发布包契约。

## 已确认决策、执行假设与约束

### 已确认决策

| 编号 | 决策 | 原因 | 来源 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| D-001 | 使用独立人工维护的 `@mb-it-org/legacy` oclif 插件承载五个命令 | 当前生成插件由 manifest 独占，直接修改会在下次生成时丢失；无生成标记的插件不会被清理 | 当前代码与用户恢复要求 | 有效 |
| D-002 | 兼容实现以 npm `@mb-it-org/cli@0.1.58` 捆绑插件源码为契约基线 | 它是用户实际安装使用的发布产物，比当前 manifest 或近似接口更权威 | 发布包核验 | 有效 |
| D-003 | 本轮不恢复旧 serve 路由 | 用户当前只要求先恢复五个命令 | 用户最新要求 | 有效 |

### 执行假设

| 编号 | 假设 | 依据 | 影响 | 验证与错误处理 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| A-001 | 旧上游接口仍可由历史网关转发 | 用户已有系统正在使用旧 CLI；本轮无生产调用授权 | 命令可恢复但目标环境接口是否仍在线不能由本地证明 | 仅验证请求契约；明确披露目标环境未验证，接口下线则后续需后端兼容任务 | 待验证 |

### 强制约束

- 遵守 `<domain> → shared ← cli`，兼容插件只依赖 shared 和 oclif，不依赖 cli。
- 只保留 GET 与查询类 POST。
- 所有人工维护类、方法和测试方法具有真实标准文档。
- 不记录或输出凭据、请求响应正文和生产数据。
- 不修改带 `AUTO-GENERATED` 标记的当前命令。

## 影响范围

### 预计修改文件

- `packages/legacy/package.json`
- `packages/legacy/tsconfig.json`
- `packages/legacy/src/index.ts`
- `packages/legacy/src/commands/{crm,product,pim}/*.ts`
- `packages/legacy/src/__tests__/legacy-commands.test.ts`
- `packages/cli/package.json`
- `pnpm-lock.yaml`
- 本任务文档、审计索引和待生效记忆入口

### 明确不修改

- 当前生成 manifest 与生成业务包。
- `D:\workspace\cli-service` 后端。
- `mbs serve` 路由和默认 API 地址。
- 用户已有的无关工作区修改。

### 关联参考

- npm `@mb-it-org/cli@0.1.58` 捆绑的 `crm`、`product`、`pim` 插件源码。
- `scripts/gen-from-manifest.mjs` 的生成标记和过期包清理逻辑。
- `.docs/memory/topics/接口语义发现.md` 与相关改造任务。

## 执行前提

- 使用现有 Node、pnpm、TypeScript、oclif 和 shared HTTP 客户端，不新增外部运行时依赖。
- 只在本地构建和测试，不需要生产凭据。
- `pnpm-lock.yaml` 开始时无既有修改；更新后必须检查只增加兼容 workspace importer 和 CLI workspace link。

## 执行步骤

### S1：核验发布契约与生成边界

- **状态**：已完成
- **目标**：确认五个命令的精确源码、请求契约和不会被生成器清理的扩展点。
- **前置条件**：无。
- **可直接执行的操作**：读取 npm 正式包、当前插件结构和生成器。
- **修改对象**：仅任务文档。
- **预期结果**：确定兼容插件方案。
- **验证方式**：比对命令 ID、flags、方法、路径和生成标记判断。
- **客观通过标准**：五个命令均有权威旧源码；人工包无 `.mbs-generated.json` 时不会进入清理集合。
- **失败处理**：无法取得精确契约时停止实现并请求用户提供旧包。
- **实际执行结果**：已从 npm `0.1.58` 捆绑依赖取得五个命令源码；确认生成器只删除带 `.mbs-generated.json` 的包。
- **验证证据**：发布包静态核验与 `findStaleGeneratedDomains`/`removeGeneratedDomain` 代码检查。

### S2：实现独立兼容插件

- **状态**：已完成
- **目标**：恢复五个旧命令且不影响当前生成命令。
- **前置条件**：S1。
- **可直接执行的操作**：新增插件、移植旧命令契约、注册 CLI workspace 插件。
- **修改对象**：`packages/legacy/**`、`packages/cli/package.json`、`pnpm-lock.yaml`。
- **预期结果**：本地 CLI 可发现五个旧命令。
- **验证方式**：TypeScript 构建、oclif 帮助发现。
- **客观通过标准**：五条 `--help` 退出码为 0，命令和 flags 可见。
- **失败处理**：保留任务文件，局部撤销本任务新增文件和注册项，不触碰既有修改。
- **实际执行结果**：新增 `@mb-it-org/legacy`，五个命令按 `0.1.58` 契约恢复；CLI 已注册 workspace 插件，锁文件只增加预期 importer/link。
- **验证证据**：兼容包和 CLI TypeScript 构建通过；五条本地 `--help` 均退出 0。

### S3：增加契约回归测试

- **状态**：已完成
- **目标**：覆盖命令存在性、flags、请求方法、路径、字段和数组转换。
- **前置条件**：S2。
- **可直接执行的操作**：新增兼容插件单元测试。
- **修改对象**：`packages/legacy/src/__tests__/legacy-commands.test.ts`。
- **预期结果**：未来修改无法静默删除或改变五个旧契约。
- **验证方式**：运行兼容插件测试。
- **客观通过标准**：正常请求和关键数组/布尔/必填 flag 断言通过。
- **失败处理**：修正实现或契约基线；不得降低断言规避失败。
- **实际执行结果**：新增发布基线冻结投影和 10 条测试，覆盖五个命令的 flag 元数据、帮助文本指纹、请求方法、路径、字段集合及数组转换。
- **验证证据**：`pnpm --filter @mb-it-org/legacy test`，1 个文件、10 条测试全部通过。

### S4：执行 V3 本地验证与差异审查

- **状态**：已完成
- **目标**：证明兼容恢复正确且没有混入既有修改。
- **前置条件**：S2、S3。
- **可直接执行的操作**：定向测试、受影响模块测试、构建、五条帮助命令、Git 差异和敏感信息检查。
- **修改对象**：任务文档状态与证据。
- **预期结果**：本地实现达到待发布状态。
- **验证方式**：`pnpm --filter @mb-it-org/legacy test`、相关 CLI 测试、`pnpm build`、五条 `--help`、`git diff`。
- **客观通过标准**：命令均退出 0，测试和构建无本次引入失败，锁文件仅含预期 workspace 变化。
- **失败处理**：核心失败则保持执行中或已阻塞，不宣称完成。
- **实际执行结果**：全 workspace 构建和测试通过；五条帮助命令通过；发布工作流/捆绑工具测试通过；未产生受跟踪 dist/skills 差异，锁文件仅有 legacy workspace 变化。
- **验证证据**：`pnpm build` 通过；`pnpm test` 共 177 条测试通过；发布工作流/捆绑 6 条测试通过；五条 `--help` 退出码均为 0。

## 验收检查

| 编号 | 验收项 | 级别 | 方式 | 预期 | 实际 | 证据 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| AC-01 | 五个旧命令可发现 | V3 | 本地 CLI `--help` | 全部退出 0 | 五条均退出 0 | 本地命令帮助输出 | 通过 |
| AC-02 | flag 契约兼容 | V3 | 单元测试与帮助比对 | 名称、类型、必填、allowNo 一致 | 五个元数据投影及帮助指纹均与 `0.1.58` 一致 | legacy 测试 5 条元数据用例 | 通过 |
| AC-03 | 请求契约兼容 | V3 | mock client 单元测试 | 方法、路径、字段、数组转换一致 | 五个请求行为用例通过 | legacy 测试 5 条请求用例 | 通过 |
| AC-04 | 构建与相关回归 | V3 | workspace build/test | 无本次引入失败 | 构建通过，177 条测试通过，发布工具 6 条测试通过 | 本地命令记录 | 通过 |
| AC-05 | 工作区隔离 | V3 | Git 差异审查 | 仅任务文件和预期锁文件变化 | 无 dist/skills 受跟踪差异；锁文件只增加 legacy importer/link | `git status` 与定向 diff | 通过 |

## 风险与回滚

| 风险 | 影响 | 控制 | 回滚 |
| :--- | :--- | :--- | :--- |
| 旧接口已在目标网关下线 | 命令可发现但执行失败 | 本轮不调用生产；交付明确标记目标环境未验证 | 后续恢复网关/后端兼容端点或暂不发布兼容插件 |
| 新旧插件产生重复命令 ID | oclif 加载结果不确定 | 当前生成包缺失五个 ID；测试枚举命令并在未来 manifest 恢复时要求显式迁移 | 从 legacy 插件移除已由生成包精确接管的单条命令 |
| 大型旧 flag 集移植遗漏 | 用户脚本参数解析失败 | 直接以发布包源码为基线并用契约测试比对字段集合 | 恢复发布源码并重新运行测试 |
| 锁文件出现无关重写 | 混入用户修改或依赖漂移 | 使用现有 pnpm，仅新增 workspace importer/link，逐段审查 diff | 用精确补丁撤销本任务锁文件片段，不重置仓库 |

## 上线前验证与上线后观察

- 上线前必须在生产等价网关执行五个脱敏、只读的最小查询，验证接口仍可达；本轮不执行。
- 灰度期间只记录命令 ID、版本、退出码和脱敏错误类型，不记录 flags、响应或用户数据。
- 观察五个命令的错误率、认证失败率、网关 404/5xx 和回退到 `0.1.58` 的用户反馈。
- 发布回滚为重新安装上一个已验证 CLI 版本；不得通过修改用户配置或删除用户文件回滚。

## 任务外发现

- 当前工作区默认 API 地址和数据库 query 路由仍存在兼容性风险，不在“先恢复五个旧命令”的范围内。
- 旧 `serve --project-apis` 五条路由仍未恢复，依赖本地 HTTP 路由的看板仍会受影响。

## Git、依赖、版本与规则记录

- 不创建或切换分支，不暂存、不提交、不推送。
- 新增一个 workspace 包，不新增外部运行时依赖。
- 已完整读取 R-01、R-02、R-03、R-04、R-05、R-06、R-07、R-08、R-09、R-10 和 `mbs` Skill；分别用于任务分级、授权、实现、输入/错误、安全、验证、文件/Git、技能及项目记忆。
- `.docs/项目规范.md` 不存在；遵守根 `AGENTS.md`、当前代码和任务文档。

## 变更记录

| 时间 | 变更 | 原因 |
| :--- | :--- | :--- |
| 2026-08-10 | 创建 L3 需求、计划和恢复入口，记录已授权范围 | 用户要求先恢复五个旧命令 |
| 2026-08-10 | 完成兼容插件、冻结契约测试、全量构建测试和五条帮助验证；状态转为待验收/待发布 | 本地核心验收全部通过，真实目标网关与发布尚未执行 |
