# 发布 1.0.2 维护版本执行计划

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 发布 1.0.2 维护版本 |
| 任务级别 | L3（公共契约、远程分支与生产 npm 发布） |
| 任务类型 | RELEASE |
| 任务状态 | 已完成 |
| 当前阶段 | 发布后核验与审计回填完成 |
| 交付状态 | 已发布 |
| 执行授权 | 已授权 |
| 授权来源 | 用户明确要求“提交并发布新的版本” |
| 授权范围 | 升级 1.0.2、提交并推送 `origin/1.0.0`、创建并推送 `maintenance-1-v1.0.2`、发布 npm `maintenance-1`；不含 master、latest、强推、删除引用或弃用旧版本 |
| 验证级别 | V3 |
| 创建时间 | 2026-08-12 |
| 最后更新时间 | 2026-08-12 |
| 需求文档 | [`./需求描述.md`](./需求描述.md) |
| 仓库与分支 | `D:\workspace\mbs-cli` / `1.0.0`，上游 `origin/1.0.0` |
| 基线 | `a14f7b0a53f6fd0184852258d8bed64a97623702` |

## 恢复执行入口

- **任务目标**：把当前维护线功能与修复作为 `1.0.2` 发布到 npm `maintenance-1`，不影响普通安装用户。
- **当前进度**：本地 V3、维护分支推送、分支 CI、维护标签、Release、npm dist-tag 和官方源安装核验全部完成。
- **下一步**：无；后续维护修复必须使用新的未发布补丁版本并重新取得发布授权。
- **阻塞**：无。
- **未验证**：真实登录态下的生产业务查询不纳入发布门禁；不得输出或保存用户凭据与生产响应。
- **重要警告**：`1.0.2` 已发布且不可覆盖；不得移动 `latest`，后续修改不得重用本次标签或版本。

## 已确认决策与约束

| 编号 | 决策 | 依据 | 状态 |
| :--- | :--- | :--- | :--- |
| D-001 | 新版本使用 `1.0.2` | npm 已存在 1.0.0/1.0.1，1.0.2 未占用 | 有效 |
| D-002 | 只发布到 `maintenance-1` | 长期维护线既有发布契约 | 有效 |
| D-003 | `latest` 必须保持 `0.1.58` | 用户要求不影响正常安装用户 | 有效 |
| D-004 | 发布标签必须指向通过 CI 的维护分支提交 | Release 工作流来源分支门禁 | 有效 |

## 执行步骤

### S1：冻结发布范围与远端状态

- **状态**：已完成
- **实际结果**：`origin/1.0.0` 未新增提交；本地领先 1 个已授权召回修复提交；`1.0.2` 未发布；用户 IDE 文件排除在范围外。

### S2：升级版本并执行本地 V3

- **状态**：已完成
- **验证**：`pnpm install --frozen-lockfile`、`pnpm build`、`pnpm test`、发布保护脚本、代表性 CLI 命令、差异与敏感信息检查。
- **实际结果**：冻结安装与全仓构建通过；脚本 12 条加 workspace 165 条共 177 条测试通过；13 条发布工作流/脚本/bundle 保护通过；CLI 显示 `1.0.2`，request/find/describe/login 帮助退出 0；`git diff --check` 通过且锁文件未变化。

### S3：精确提交并推送维护分支

- **状态**：已完成
- **通过标准**：只提交任务范围文件；`origin/1.0.0` 包含发布提交；GitHub 分支 CI 成功。
- **实际结果**：首次召回修复提交 `a14f7b0` 与发布提交 `8d2c3bbf9d0059678b6d40fae9188701e8c166d9` 已普通推送；用户 `.idea` 修改未提交；分支 CI `31581204537` 成功。

### S4：创建维护标签并发布 npm

- **状态**：已完成
- **通过标准**：`maintenance-1-v1.0.2` 指向发布提交；Release 成功；npm 精确版本与 dist-tag 正确。
- **实际结果**：轻量标签 `maintenance-1-v1.0.2` 指向 `8d2c3bb`；Release `31581339414` 成功；npm 官方源为 `maintenance-1=1.0.2`、`latest=0.1.58`。

### S5：核验官方源产物并回填审计

- **状态**：已完成
- **通过标准**：官方源运行版本及 find/describe/request 入口通过；记录提交、CI、Release、标签和 dist-tag 证据并推送纯文档提交。
- **实际结果**：从 npm 官方源安装精确版本后，`mbs --version` 输出 `1.0.2`，request/find/describe 帮助通过；安装包包含 `/cli/gateway/cli-service` 和 `find → describe → request` Skill。验证依赖已卸载，未读取真实凭据或发送业务请求。

## 验收检查

| 编号 | 验收项 | 实际 | 状态 |
| :--- | :--- | :--- | :--- |
| AC-01 | 本地 V3 | frozen install、构建、177 条测试、13 条发布保护和代表性命令通过 | 通过 |
| AC-02 | 维护分支门禁 | `8d2c3bb` 属于 `origin/1.0.0`，CI `31581204537` 成功 | 通过 |
| AC-03 | 维护发布 | 标签指向正确提交，Release `31581339414` 成功 | 通过 |
| AC-04 | 通道隔离 | `maintenance-1=1.0.2`，`latest=0.1.58` | 通过 |
| AC-05 | 官方源产物 | 版本、request/find/describe、召回路由与 Skill 内容核验通过 | 通过 |

## 风险与处置

| 风险 | 控制 | 失败处置 |
| :--- | :--- | :--- |
| 意外影响普通安装用户 | 标签前后读取 npm dist-tags | 立即停止，不通过移动 latest 补救 |
| 动态请求扩大为写操作 | 命令仅接受 GET/POST，文档限定查询类 POST | 通过新增普通修复提交收紧，不改历史 |
| 分支或发布工作流失败 | 本地 V3、分支 CI、Release 三道门禁 | 保留失败证据，停止后续写入并修复 |
| 混入用户 IDE 状态 | 显式列出暂存路径并检查 staged diff | 提交前撤出暂存，不覆盖用户文件 |

## 变更记录

| 时间 | 变更 | 原因 |
| :--- | :--- | :--- |
| 2026-08-12 | 创建发布需求、计划并选择 1.0.2 | 用户授权提交发布；确认下一补丁版本未占用 |
| 2026-08-12 | 完成版本升级与本地 V3 | 在远程写入前验证安装、构建、177 条测试、13 条发布保护和代表性命令 |
| 2026-08-12 | 推送 `8d2c3bb`，分支 CI `31581204537` 成功 | 通过维护分支门禁后才创建发布标签 |
| 2026-08-12 | 推送 `maintenance-1-v1.0.2`，Release `31581339414` 成功 | 发布到维护通道且不影响 latest |
| 2026-08-12 | 完成 npm dist-tag 和官方源安装核验 | 确认实际 registry 产物而非仅工作流状态 |
