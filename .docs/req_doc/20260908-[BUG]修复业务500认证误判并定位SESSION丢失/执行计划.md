# 执行计划：修复业务 500 认证误判并定位 SESSION 异常

> 安全提示：所有诊断证据只保留布尔量、数量、退出码和脱敏错误类型；禁止输出或记录凭据值。

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 级别 | L3 |
| 类型 | BUG / SECURITY |
| 状态 | 本地已完成，待目标环境验收 |
| 交付状态 | 待发布 |
| 执行授权 | 已授权 |
| 明确排除 | 凭据披露、远程写入、Git 交付、发布部署、覆盖既有未知修改 |
| 验证级别 | V3 |
| 创建/更新时间 | 2026-09-08 |
| 需求文档 | [需求描述.md](./需求描述.md) |
| 分支与基线 | `1.0.0` / `17c92952e620564a1d34f6a7e5f75f80db286e7c` |

## 恢复执行入口

- 目标：让业务 500 保持普通 API 错误；让 ERP 缺失用户返回明确 601；辨明截图中新旧 SESSION 的实际语义。
- 已确认：auth-center 能识别并复用 CLI 当前 SESSION；没有网关响应 `Set-Cookie` 丢失证据。dev Nacos 未发现 `erpmonitor`，网关回退非本机 legacy 链路；同一 SESSION 在该远程 ERP 链路中缺少用户，剩余问题属于目标运行态。
- 下一步：发布三处本地修复后，在目标环境逐跳核对业务路由的请求 Cookie、ERP 实例版本以及 Spring Session 配置覆盖；证据只记录布尔量与实例版本，不记录凭据。
- 工作区警告：`technical-center` 和 `ingroup-erp` 有其他既有修改，均视为用户所有。

## 决策

| 编号 | 内容 | 状态 |
| :--- | :--- | :--- |
| D-001 | 仅 HTTP 401 或业务 401/601 允许进入一次认证刷新 | 已确认 |
| D-002 | compat-session 在无效/异用户会话时轮换 SESSION，在有效同用户会话时复用且可不发 Set-Cookie | 已确认 |
| D-003 | current-user 识别当前 SESSION，但远程 ERP 请求缺少用户；不再归因于响应头丢失 | 已确认 |
| D-004 | ERP 缺少用户必须返回 601，不能用空指针 500 充当认证信号 | 已确认 |
| C-001 | 不读取、记录、散列或输出凭据值 | 有效 |
| C-002 | 不覆盖其他仓库的既有差异，不执行 Git 交付或远程部署 | 有效 |

## 执行步骤

### S-01：固定脱敏证据

- 状态：已完成
- 结果：原命令和最小命令均复现；确认 CLI 请求只有一个 SESSION、无重定向；确认 current-user 识别成功而 ERP 返回 500。

### S-02：修复 CLI 错误分类

- 状态：已完成
- 红测：旧实现把 500 构造成 `NotAuthenticatedError`，且调用 refresh。
- 修改：从业务码处理表移除 500；补充有/无消息的普通 `MBSError` 契约和不刷新测试。
- 结果：定向 22/22、shared 152/152、全仓 306/306；14 个 workspace 构建通过。

### S-03：核对并修复服务端边界

- 状态：已完成
- auth-center：确认已有会话复用、必要轮换和用户快照写入正确，无源码修改。
- erp-monitor：红测精确复现空用户 NPE；改用 `getSession(false)` 并在业务计算前返回 601。
- api-gateway：红测确认剩余链调用两次；改为一次调用后的本地 CORS 清理，保持多值 `Set-Cookie`。

### S-04：V3 验证和收口

- 状态：已完成
- ERP：相关 reactor 8/8 测试及跳过测试打包通过（Corretto JDK 8）。
- 网关：模块 23/23 测试及跳过测试打包通过。
- 原命令复跑：仍收到远程业务 500，但已按普通 API 错误退出 1，且认证缓存时间戳不因该 500 改写。
- 差异：仅审查本任务目标文件；未暂存、提交、发布或覆盖其他修改。

## 剩余风险与上线门禁

- 原命令没有经过本机完整网关链路；本地源码修复尚未部署，不能宣称线上命令已恢复。
- 发布后必须在每个目标 ERP 实例上核对版本、Redis Session 有效配置及业务路由实际收到唯一 Cookie 的布尔结果；不得记录 Cookie 值。
- 若发布后的第一次 ERP 响应为 601，CLI 会按契约刷新一次；刷新后的同一请求仍为 601 时应直接保留最终后端响应，不得无限重试。
- 仓库中另发现历史源码硬编码 SESSION 的安全风险，本任务未读取其值或擅自修改；需要立即轮换并另立清理任务。

## 变更记录

- 2026-09-08：完成跨仓库诊断、三处精确红绿修复、本地 V3 验证和审计收口；保留目标运行环境联调门禁。
