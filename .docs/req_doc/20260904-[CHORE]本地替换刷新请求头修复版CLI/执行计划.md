# 执行计划：本地替换刷新请求头修复版 CLI

> 安全提示：验证仅检查命令、路径、链接与公开帮助，不执行真实认证，不记录任何凭据。

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 本地替换刷新请求头修复版 CLI |
| 级别 | L3 |
| 类型 | CHORE / SECURITY |
| 任务状态 | 已完成 |
| 当前阶段 | 本机替换与离线验证完成 |
| 交付状态 | 不适用（仅本机替换，不发布） |
| 执行授权 | 已授权 |
| 授权来源 | 用户明确要求“将本地的mbs-cli替换为你修改后的版本” |
| 授权范围 | 将本机活动的系统级 npm 安装替换为工作区 `packages/cli` 链接并执行离线验证 |
| 验证级别 | V3 |
| 创建/更新时间 | 2026-09-04 |
| 需求文档 | [需求描述.md](./需求描述.md) |
| 分支与基线 | `1.0.0` / `168a05eb6669c4abf1fef804f912b05dac857a94`；工作区包含前置任务的未提交修改 |
| 执行环境 | Windows / PowerShell / npm 全局前缀 `D:\Program Files\nodejs` |

## 恢复执行入口

- **任务目标**：让本机当前活动的 `mbs` 命令直接运行工作区中已修复的版本。
- **当前进度**：活动系统级 npm 包已替换为工作区 Junction；构建产物、命令路径、链接目标、版本、refresh 帮助和内置 Skill 均核验通过。
- **下一步**：如需让其他机器或 npm 用户生效，另行授权提交、推送和维护版本发布。
- **阻塞**：无。
- **未验证**：真实认证服务；本任务按安全边界明确不执行。
- **重要警告**：不得运行 `mbs update`，否则会按 npm 发布通道安装而不是使用当前工作区修复。
- **当前允许操作**：只读检查与交付说明。
- **当前禁止操作**：继续修改已完成任务、真实认证请求、凭据读取、用户级副本改动、Git 写入与正式发布。

## 已确认决策与约束

| 编号 | 类型 | 内容 | 依据 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| D-001 | 决策 | 只替换 PATH 当前命中的系统级 npm 副本 | 最小充分实现与命令解析结果 | 有效 |
| D-002 | 决策 | 使用 `npm link` 指向工作区 `packages/cli` | 项目既有本地安装流程 | 有效 |
| D-003 | 决策 | 不运行真实 refresh，以免读取和发送本机凭据 | 认证安全边界 | 有效 |
| C-001 | 约束 | 被 PATH 遮蔽的用户级 npm 副本保持不变 | 用户只要求替换本地活动命令 | 有效 |
| C-002 | 约束 | 包版本仍显示 `1.1.1`，链接目标是判断工作区版本生效的权威证据 | 工作区 package.json 未改版本 | 有效 |

## 执行步骤

| 步骤 | 状态 | 目标 | 验证与通过标准 | 失败处理 |
| :--- | :--- | :--- | :--- | :--- |
| S-01 审计与现状 | 已完成 | 固定授权、活动安装、范围和回滚命令 | 审计可恢复且不含敏感信息 | 停止替换并补齐事实 |
| S-02 构建产物核验 | 已完成 | 确认工作区产物包含修复且 CLI 可启动 | 编译产物未赋值交换接口分类头；工作区版本与 refresh 帮助成功 | 重新构建或停止替换 |
| S-03 本地替换 | 已完成 | 将系统级全局包切换为工作区链接 | npm 写入链接后主进程悬挂并由工具超时终止；链接目标准确，npm 清单和命令执行均通过 | 使用回滚命令恢复官方版本 |
| S-04 离线验证 | 已完成 | 验证活动命令和内置 Skill | 路径、链接、版本、帮助与 Skill 全部通过 | 分类诊断并回滚或修复 |
| S-05 审计收口 | 已完成 | 回填本机状态、Git 状态和未验证项 | 文档与现场一致 | 保持任务执行中并披露阻塞 |

## 验证清单

| 编号 | 验收项 | 方式 | 状态 |
| :--- | :--- | :--- | :--- |
| AC-01 | 工作区编译产物包含请求头修复 | 静态检查 `packages/shared/dist/auth/session-login.js` | 通过；唯一文本命中为“不发送该头”的方法文档，无请求赋值 |
| AC-02 | 工作区 CLI 可启动 | 直接运行工作区 `bin/run.js` 的版本和 refresh 帮助 | 通过 |
| AC-03 | 活动全局包指向工作区 | 检查系统级包目录 LinkType 与 Target | 通过；Junction 目标为工作区 `packages/cli` |
| AC-04 | PATH 命令使用链接后的 CLI | `Get-Command mbs`、npm 全局清单与 `mbs --version` | 通过；系统级 shim 命中链接包，版本输出 `1.1.1` |
| AC-05 | refresh 帮助与内置 Skill 可读 | 执行公开、无网络的帮助、Skill path 和 Skill show 命令 | 通过；Skill path 指向工作区，更新与全局文档可读 |
| AC-06 | 无越界变更 | 检查 Git 状态和用户级副本 | 通过；用户级链接时间保持在本任务之前，未触发认证、发布或 Git 写入 |

## 验证记录

- 工作区 `packages/shared/dist/auth/session-login.js` 中没有 `client-type` 请求头赋值；唯一同名文本是“不发送该头”的方法文档。普通业务 `dist/http.js` 仍保留分类头。
- 直接运行工作区 `node packages/cli/bin/run.js --version` 与 `refresh --help` 成功。
- `npm link` 已创建系统级 Junction，但 npm 主进程在无子进程、无新增输出时未自行退出，最终由执行工具以超时码 `124` 终止。此异常未被当作成功退出证据，改以文件系统、npm 清单和实际命令三类后置条件核验。
- 系统级全局包 LinkType 为 Junction，目标为 `D:\workspace\mbs-cli\packages\cli`；`npm list -g` 显示同一工作区链接。
- `Get-Command mbs` 仍命中 `D:\Program Files\nodejs\mbs.ps1`；`mbs --version`、`mbs refresh --help` 均成功。
- `mbs skills path` 返回工作区 `packages/cli/skills`；主 Skill、更新 Skill 和全局认证契约均可从活动命令读取。
- `packages/cli` 使用的 shared 依赖也是指向工作区 `packages/shared` 的 Junction，因此活动命令加载本次修复构建产物。
- 未运行 login、refresh、whoami、version 联网检查或业务命令；未读取本机认证缓存。
- 被 PATH 遮蔽的用户级副本是本任务前已存在的工作区 Junction，时间为 2026-07-23，本任务未修改。

## 变更记录

- 2026-09-04：用户授权将本机 `mbs-cli` 替换为工作区修改版。
- 2026-09-04：确认当前活动版本为系统级 npm 安装的 `1.1.1`；用户级 npm 副本被 PATH 遮蔽且不在范围内。
- 2026-09-04：确定使用 `npm link`，回滚为重新安装官方 `1.1.1`。
- 2026-09-04：工作区编译产物与 CLI 版本、refresh 帮助核验通过；进入本地链接替换。
- 2026-09-04：系统级 npm 包已指向工作区；尽管 npm 主进程未正常退出，链接、清单、命令、依赖与 Skill 后置条件全部通过。
- 2026-09-04：完成审计收口；真实认证、Git 写入和正式发布未执行。
