# 发布 1.0.6 维护版本执行计划

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 发布 1.0.6 维护版本 |
| 任务级别 | L3（认证安全边界、远程分支与生产 npm 发布） |
| 任务类型 | RELEASE |
| 任务状态 | 执行中 |
| 当前阶段 | 精确提交与维护分支 CI |
| 交付状态 | 待发布 |
| 执行授权 | 已授权 |
| 授权来源 | 用户明确要求“提交代码并发布1.0.6” |
| 授权范围 | 升级 1.0.6、精确提交并普通推送 `origin/1.0.0`、创建并推送 `maintenance-1-v1.0.6`、通过既有工作流发布 npm `maintenance-1`；不含 `.idea`、master、latest、强推、删除引用、覆盖旧版本或真实业务请求 |
| 计划完成后动作 | 当前范围内持续执行至发布后核验和审计回填完成 |
| 验证级别 | V3 |
| 创建和更新时间 | 2026-09-03 |
| 需求文档 | [`./需求描述.md`](./需求描述.md) |
| 仓库与分支 | `D:\workspace\mbs-cli` / `1.0.0`，上游 `origin/1.0.0` |
| 基线 | `6e067639072f67f00b1390c8d3e04102a5b6b44c` |
| 执行环境 | Windows / PowerShell / Node 20 兼容项目工具链 / pnpm 10.28.2 |

## 恢复执行入口

- **任务目标**：把当前认证安全整改作为 `1.0.6` 发布到 npm `maintenance-1`，不移动普通 `latest`。
- **当前进度**：版本已升级到 1.0.6，发布前本地 V3 和发布保护通过，准备精确提交推送。
- **下一步**：精确提交并普通推送 `origin/1.0.0`，等待分支 CI 成功后推送维护标签。
- **阻塞**：无。
- **待确认**：无；用户已明确指定提交和发布 1.0.6。
- **未验证**：分支 CI、Release、npm 产物、真实 HTTPS 网关与真实凭据。
- **重要警告**：npm 已发布版本不可覆盖；标签只能在分支 CI 成功后创建；`.idea/claudeCodeTabState.xml` 必须保留且不提交；不得记录真实凭据。
- **执行授权**：已授权。
- **当前允许操作**：当前认证范围的版本更新、文档、精确提交、普通推送、维护标签、工作流与 npm 官方源核验。
- **当前禁止操作**：修改或推送 master、移动 latest、强推、删除或覆盖引用、提交 `.idea`、真实业务请求、读取或记录敏感信息。
- **授权后的第一步**：已核对仓库、分支、基线、上游、工作区、已暂存用户修改和版本占用状态。

## 已确认决策、假设与约束

| 编号 | 类型 | 内容 | 依据 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| D-001 | 决策 | 使用未占用的 `1.0.6` 与 `maintenance-1-v1.0.6` | 用户明确指定，npm 与远端查询均未占用 | 有效 |
| D-002 | 决策 | 只发布到 npm `maintenance-1`，保持 `latest=0.1.59` | 维护分支既有发布契约 | 有效 |
| D-003 | 决策 | 维护标签只指向已通过分支 CI 的 `origin/1.0.0` 提交 | Release 工作流门禁与历史发布流程 | 有效 |
| D-004 | 决策 | 使用精确路径提交，保留 `.idea/claudeCodeTabState.xml` 内容和既有暂存状态 | 用户修改归属不属于当前任务 | 有效 |
| A-001 | 假设 | 不使用真实凭据和真实目标网关完成发布验收 | 自动化、帮助与官方包结构足以覆盖发布产物，真实协议联调作为剩余风险 | 有效 |

## 影响范围

- **预计修改**：`packages/cli/package.json`、当前认证实现和测试、README/Skill/项目规范、认证记忆、关联功能任务及本发布任务文档。
- **明确不修改**：`.idea/claudeCodeTabState.xml`、master、latest、生产配置、真实用户缓存、其他仓库与外部业务系统。
- **关联参考**：[`../20260903-[FEATURE]接入长短Token刷新登录/`](../20260903-[FEATURE]接入长短Token刷新登录/)、[`../20260902-[SECURITY]禁止持久化MBS_KEY/`](../20260902-[SECURITY]禁止持久化MBS_KEY/)、[`../../memory/topics/MBS认证凭据边界.md`](../../memory/topics/MBS认证凭据边界.md)。

## 执行步骤

| 步骤 | 状态 | 目标 | 验证与失败处理 |
| :--- | :--- | :--- | :--- |
| S-01 冻结范围与远端状态 | 已完成 | 核对版本、标签、分支、工作区与用户修改 | 本地和远端基线一致；1.0.6 未占用；`.idea` 已明确排除 |
| S-02 升级版本与本地 V3 | 已完成 | 更新 CLI 版本并执行 frozen install、构建、测试、发布保护、帮助、差异与敏感检查 | frozen install、14 包构建、238 项全仓测试、12 项发布保护、manifest dry-run 和命令帮助通过 |
| S-03 精确提交和维护分支 CI | 进行中 | 只提交确认范围，普通推送 `origin/1.0.0` 并等待 CI | 推送冲突或 CI 失败时不强推、不创建标签 |
| S-04 维护标签与 npm 发布 | 未开始 | 推送新维护标签并等待 Release 成功 | 标签或发布失败时不复用/覆盖版本，保留证据并调查 |
| S-05 官方源核验与审计回填 | 未开始 | 核验 dist-tag 和隔离安装产物，更新任务/记忆并推送回填 | 若包已发布但核验失败，明确状态并不得覆盖 1.0.6 |

## 验收检查

| 编号 | 验收项 | 当前状态 |
| :--- | :--- | :--- |
| AC-01 | 本地 V3 和发布保护 | 通过 |
| AC-02 | 提交范围与 `.idea` 隔离 | 待验证 |
| AC-03 | 维护分支提交、推送与 CI | 待验证 |
| AC-04 | 维护标签与 Release | 待验证 |
| AC-05 | `maintenance-1=1.0.6` 且 `latest=0.1.59` | 待验证 |
| AC-06 | 官方源隔离安装与认证产物 | 待验证 |

## 风险、数据保护与回滚

| 风险 | 控制 | 回滚或失败处置 |
| :--- | :--- | :--- |
| 认证协议与目标网关实际属性不一致 | 自动化覆盖协议解析与失败关闭；披露未做真实联调 | 发布后用新补丁普通反向提交，禁止覆盖 1.0.6 |
| 长期凭据或错误正文泄漏 | 不读取真实凭据；检查源码、测试、文档和 staged diff | 发现敏感信息立即停止提交与发布并清除未发布证据 |
| 意外移动普通安装通道 | 只使用维护标签并在前后核验 dist-tags | 立即停止，不修改 latest |
| 混入用户 IDE 状态 | 精确路径提交并核对提交对象 | 提交前停止；保留 `.idea` 原内容与暂存状态 |
| 分支或工作流失败 | 本地 V3、分支 CI、Release 三道门禁 | 禁止强推和复用版本；使用普通追加提交或新补丁 |

## Git、依赖、安全与规则记录

- 仓库：`D:\workspace\mbs-cli`；分支：`1.0.0`；远端目标：`origin/1.0.0`；基线：`6e067639`。
- 计划 Git 操作：精确暂存当前范围、提交、普通推送；CI 成功后创建并推送一个新轻量维护标签；发布后提交并推送审计回填。
- 当前 `.idea/claudeCodeTabState.xml` 是用户已暂存修改；不得改动、取消暂存或提交。
- 不新增或升级依赖；使用现有 lockfile 和项目固定工具链。
- 不读取登录态、Cookie、Token 或真实请求响应；证据仅保留版本、计数、提交与工作流状态。
- 已完整读取 R-01 至 R-10 中本任务触发的 R-01/R-02/R-03/R-04/R-05/R-06/R-07/R-08/R-09/R-10；使用 `mbs` 约束认证交付，使用 `graphify` 导航提交关系并以当前源码复核。
- 当前平台禁止主动子代理，且发布步骤共享 Git、版本和远程状态，因此串行执行。

## 任务外发现

- `graphify` 认证图谱落后于当前工作区，仍包含已删除的旧 refresher/key 读取节点；只用于导航，发布范围以源码、测试和 staged diff 为准。

## 变更记录

| 时间 | 变更 | 原因 |
| :--- | :--- | :--- |
| 2026-09-03 | 创建发布需求和计划，确认 1.0.6 未占用 | 用户明确授权提交并发布 1.0.6 |
| 2026-09-03 | 升级版本并完成发布前本地 V3 | frozen install、14 包构建、238 项测试、12 项发布保护、manifest dry-run 与 login/refresh/whoami 帮助通过 |
