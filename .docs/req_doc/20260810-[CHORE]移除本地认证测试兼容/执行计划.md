# 执行计划：移除本地认证测试兼容

## 0. 任务元信息与恢复入口

| 字段 | 内容 |
| :--- | :--- |
| 任务级别 | L3（认证配置与跨模块安全边界回滚） |
| 任务类型 | CHORE |
| 任务状态 | 已完成 |
| 当前阶段 | 代码、配置、进程、全量测试、构建、link 与 Skill 安装均已完成 |
| 交付状态 | 本地已验证 / 待发布 |
| 执行授权 | 已授权 |
| 授权来源 | 用户明确要求本地测试结束后清除兼容改动并按线上标准完成 |
| 授权范围 | shared、cli、doris、Skill、任务审计与项目记忆；清理本机非敏感开发配置并停止本次联调进程 |
| 计划完成后动作 | 当前范围内继续实施并验证 |
| 验证级别 | V3 |
| 创建/更新时间 | 2026-08-10 |
| 需求文档 | [`./需求描述.md`](./需求描述.md) |
| 分支与基线 | 当前工作区存在大量既有修改，只做外科手术式清理，不覆盖或暂存其他修改 |

- **任务目标**：生产构建仅保留正常认证和线上地址，不携带本地测试专用入口。
- **当前进度**：本地测试兼容已完整移除，线上地址和普通认证已恢复，验证通过。
- **下一步**：按独立发布流程发布；本任务未提交、推送或发布。
- **阻塞**：无。
- **重要警告**：不得读取、复制或输出用户此前提供的 Cookie。

## 1. 已确认决策、假设与约束

- D-001：删除本地认证代理与 Cookie Prompt，不保留隐藏开关或环境变量兼容。
- D-002：`find`、`describe` 和数据库查询统一回到保存登录态与标准网关。
- D-003：正式的本地 `mbs serve` 是产品能力，不属于认证联调兼容，继续保留。
- D-004：恢复线上默认 API 地址 `http://www.instudio.me:6206`。
- 强制约束：只读、不泄露凭据、不修改前端配置、不提交、不推送。

## 2. 执行步骤

1. [已完成] 调整测试以声明线上认证、地址和命令契约；确认旧实现先失败。
2. [已完成] 删除 shared、cli 与 doris 的本地代理、提示和地址覆盖实现。
3. [已完成] 清理 Skill、本机配置和环境变量，停止本次 4174 联调进程。
4. [已完成] 定向 49 项、全量 158 项测试通过；两次全量构建通过；全局 link 和 Skill 安装验证通过。
5. [已完成] 更新审计索引、项目记忆并核对 Git 差异边界。

## 3. 验收检查

- V-001：源码与发布文档不含本地认证入口。
- V-002：默认配置和当前本机配置均使用线上地址。
- V-003：正式查询命令仍通过保存认证与标准网关工作。
- V-004：全量测试与构建通过。
- V-005：差异和输出不含凭据或无关修改。

## 4. 已加载规则

- 已读取 L3 所需核心执行、任务审计、会话交接和项目记忆规则。
- 已读取 `mbs` Skill；本任务不访问业务数据。

## 5. 变更记录

- 2026-08-10：创建清理任务，确定完整移除本地认证兼容并保留正式只读能力。
- 2026-08-10：线上契约测试先失败后通过；删除源码和旧 dist 残留，恢复线上默认地址。
- 2026-08-10：本机 `devLoginUrl` 与两个开发环境变量已清理，4174 Vite 进程已验证归属后停止；未读取 Cookie。
- 2026-08-10：全量 158 项测试、两次全量构建、命令帮助、全局 link、Codex Skill 哈希与线上配置验证通过；未提交、推送或发布。
