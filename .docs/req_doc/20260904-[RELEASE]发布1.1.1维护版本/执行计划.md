# 执行计划：发布 1.1.1 维护版本

> 安全提示：发布证据只记录脱敏状态、版本、提交和作业标识，不得记录真实账号、密码、Cookie、Token、请求响应正文或签名地址。

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 发布 1.1.1 维护版本 |
| 任务级别 | L3 |
| 任务类型 | RELEASE / SECURITY |
| 任务状态 | 已完成 |
| 当前阶段 | 已完成并归档 |
| 交付状态 | 已发布并完成审计核验 |
| 执行授权 | 已授权 |
| 授权来源 | 用户明确要求“提交代码并发布1.1.1” |
| 授权范围 | 当前认证修复与审计、版本 1.1.1、普通推送 `origin/1.0.0`、维护标签、npm `maintenance-1` 发布及发布后核验和审计回填 |
| 计划完成后动作 | 持续执行至发布后官方源核验、Skill 刷新和审计回填完成 |
| 验证级别 | V3 |
| 创建/更新时间 | 2026-09-04 |
| 需求文档 | [需求描述.md](./需求描述.md) |
| 仓库与分支 | `D:\workspace\mbs-cli` / `1.0.0` / `origin/1.0.0` |
| 基线 | `dc29e59efd7acbdc8354e9a0b41639d947fdf5f5` |
| 执行环境 | Windows / PowerShell / Node 20 兼容项目工具链 / pnpm 10.28.2 |

## 恢复执行入口

- **任务目标**：提交 current-user 请求头修复并把 `@mb-it-org/cli@1.1.1` 发布到 npm `maintenance-1`。
- **当前进度**：功能提交、版本提交、两次分支 CI、维护标签、Release、npm 官方源、官方包、Skill 刷新及审计提交 CI 均已完成。
- **下一步**：无；真实认证中心联调需另行授权。
- **阻塞**：无。
- **待确认**：无；沿用 1.x 维护分支契约，只移动 `maintenance-1`，保持 `latest` 不变。
- **未验证**：真实认证服务联调未执行。
- **重要警告**：`pnpm release:patch` 只支持 `master/latest`，不得用于当前维护分支；版本提交和 `maintenance-1-v1.1.1` 必须手工按既有流程创建。
- **执行授权**：已授权。
- **当前允许操作**：任务范围内编辑、暂存和提交，普通推送维护分支，创建推送维护标签，监控 GitHub Actions 与 npm，发布后更新并提交审计。
- **当前禁止操作**：强推、改写历史、删除引用、修改 master/latest、覆盖旧 npm 版本、使用真实凭据请求。
- **授权方式**：当前用户消息已直接授权提交与发布 1.1.1。
- **授权后的第一步**：已核对仓库、分支、基线、工作区、远端、版本、标签和 npm dist-tag。

## 已确认决策与约束

| 编号 | 类型 | 内容 | 依据 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| D-001 | 决策 | 当前工作区认证修复、测试、审计与记忆统一纳入功能提交 | 用户授权及工作区归属核对 | 有效 |
| D-002 | 决策 | 版本为 `1.1.1`，标签为 `maintenance-1-v1.1.1` | 用户指定版本与维护分支工作流 | 有效 |
| D-003 | 决策 | npm 只更新 `maintenance-1`，保持 `latest=0.1.59` | 1.x 长期维护分支契约 | 有效 |
| D-004 | 约束 | 仅普通推送，不改写、删除或覆盖历史和已发布版本 | R-08 与发布安全边界 | 有效 |
| D-005 | 约束 | 不使用或记录真实凭据 | R-06 与项目认证规范 | 有效 |

## 影响范围

- current-user 请求头源码与测试。
- 关联修复任务、审计索引和认证主题记忆。
- 本发布任务文档与 `packages/cli/package.json`。
- 发布后更新 1.x 维护分支记忆、认证记忆和两个任务的交付状态。
- 明确不修改：master、latest、依赖、锁文件、真实认证缓存、生产配置和其他仓库。

## 执行前提与安全专项

- 受保护资产：npm 发布权限、GitHub 维护分支和标签、认证凭据与发布制品完整性。
- 不可信输入：远端分支状态、npm registry 查询、CI/Release 状态；发布前逐项校验，不根据单次缓存结果推断成功。
- 外部写入顺序：功能提交及分支 CI → 版本提交及分支 CI → 维护标签 → Release/npm → 官方包核验 → 审计回填。
- 失败行为：任一前置验证或 CI 失败即停止后续不可逆步骤；不重试掩盖失败，不强推。
- 凭据处理：发布命令不携带 npm Token；仅 GitHub Actions 使用仓库 Secret。
- 回滚：标签前使用普通修复提交；npm 发布后只允许更高版本前向修复。

## 已加载规则

| 规则 | 触发原因 | 状态 | 影响步骤 |
| :--- | :--- | :--- | :--- |
| R-01 | 全部任务 | 已完整读取 | 全部 |
| R-02 | L3 发布建档 | 已完整读取 | S-01、S-06 |
| R-03 | 执行型 L3 与远程步骤 | 已完整读取 | 全部 |
| R-04 | 代码、测试和发布文档审查 | 已完整读取 | S-01、S-02 |
| R-05 | 认证输入、异常与资源边界 | 已完整读取 | S-01、S-02 |
| R-06 | 认证和发布敏感信息 | 已完整读取 | 全部 |
| R-07 | V3、CI 与生产发布 | 已完整读取 | S-02 至 S-06 |
| R-08 | Git、版本、标签和远程写入 | 已完整读取 | 全部 |
| R-09 | MBS 技能和 Skill 刷新 | 已完整读取 | S-02、S-06 |
| R-10 | 认证与维护分支记忆 | 已完整读取 | S-01、S-06 |
| MBS Skill / global / update | MBS 维护版本发布与 Skill 协议 | 已完整读取 | S-02、S-06 |

## 执行步骤

| 步骤 | 状态 | 目标 | 前置条件 | 操作与修改对象 | 通过标准 | 失败处理 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| S-01 冻结范围与审查 | 已完成 | 确认所有待提交改动均属于当前修复和发布 | 用户授权、基线一致 | 差异、工作区、任务文档、远端与版本只读检查 | 9 个文件和 2 个任务目录均可追溯；无未知修改、凭据或目标占用 | 停止提交并调查归属或冲突 |
| S-02 发布前 V3 | 已完成 | 验证依赖、测试、构建、发布守卫和 CLI | S-01 | frozen install、全仓测试构建、发布保护、CLI 冒烟、安全检查 | frozen install、259 项测试、14 包构建、workflow 守卫、release script 7 项、bundle 5 项与 CLI/Skill 冒烟通过 | 分类失败并修复后完整复验 |
| S-03 功能提交与 CI | 已完成 | 提交并推送认证修复 | S-02 | 精确暂存当前修复与审计；普通推送 `origin/1.0.0` | 提交 `2e81e1e` 已推送；分支 CI `33840535323` 成功 | 不更新版本、不打标签；普通修复提交 |
| S-04 版本提交与 CI | 已完成 | 升级并提交 `1.1.1` | S-03 CI 成功 | `packages/cli/package.json`、发布任务状态 | 版本提交 `fc3702f` 已推送；分支 CI `33840660399` 成功 | 不打标签；普通修复提交 |
| S-05 标签与 npm 发布 | 已完成 | 推送维护标签并等待 Release | S-04 CI 成功 | `maintenance-1-v1.1.1` | 标签精确指向 `fc3702f`；Release `33840769312` / 作业 `100922429982` 成功；npm 精确版本可见 | 保留可审计状态，停止后续声明并调查 |
| S-06 官方源核验与回填 | 已完成 | 验证官方包、刷新 Skill 并同步发布事实 | S-05 | npm dist-tag、临时执行、MBS Skill、任务与记忆文档 | 官方包版本、登录帮助与 Skill 通过；Codex Skill 已刷新；审计提交 `e4c5631` 的 CI `33841934200` 成功 | 标记待验收或阻塞，不虚报发布完成 |

## 验收检查

| 编号 | 验收项 | 状态 |
| :--- | :--- | :--- |
| AC-01 | 全部改动审查与敏感信息检查 | 通过：当前 9 个文件均可追溯，未发现真实凭据或无关产物 |
| AC-02 | frozen install、259 项或更多全仓测试、14 包构建 | 通过：259 项测试、14 包构建，lockfile 无变化 |
| AC-03 | 发布 workflow、release script/bundle 守卫与 CLI 冒烟 | 通过：workflow 守卫、7 项 script、5 项 bundle 和 login/Skill 冒烟成功 |
| AC-04 | 功能提交、版本提交和两次分支 CI | 通过：`2e81e1e` / CI `33840535323` 与 `fc3702f` / CI `33840660399` 均成功 |
| AC-05 | 维护标签、Release、npm 精确版本与 dist-tag | 通过：标签指向 `fc3702f`；Release `33840769312` 成功；`maintenance-1=1.1.1`、`latest=0.1.59` |
| AC-06 | 官方包、Skill 刷新和审计回填 | 通过：官方包版本、登录帮助、主 Skill 与 update Skill 可读，Codex Skill 已刷新；审计提交 `e4c5631` 与 CI `33841934200` 成功 |

## 风险、失败处理与观察

- 若差异出现真实凭据、未完成核心缺口或验证失败，停止提交和发布。
- 若远端分支前进，停止并调查；不强推、不自动覆盖。
- 若分支 CI 失败，不进入下一阶段；若 Release 失败，保留标签与作业入口并按原因处置。
- npm 发布成功后不可覆盖同版本；严重缺陷使用更高补丁版本。
- 发布后观察 GitHub Actions 结论、npm 精确版本、`maintenance-1` 和 `latest`、官方包版本及 Skill 可读性。

## 任务外发现

- 无。

## 变更记录

- 2026-09-04：用户授权提交当前修复并发布 `1.1.1`；确认目标版本和维护标签未占用，任务进入执行中。
- 2026-09-04：完成差异归属、远端、目标版本、标签与敏感信息预检；未发现阻塞，进入发布前 V3。
- 2026-09-04：frozen install、全仓 259 项测试、14 包构建、发布守卫和 CLI/Skill 冒烟全部通过；进入功能提交阶段。
- 2026-09-04：功能提交 `2e81e1e` 普通推送到 `origin/1.0.0`；分支 CI `33840535323` 成功，进入版本提交阶段。
- 2026-09-04：版本更新为 `1.1.1`；本地 CLI 版本、14 包构建和 workflow 守卫通过，目标 npm 版本与维护标签仍未占用。
- 2026-09-04：版本提交 `fc3702f` 普通推送到 `origin/1.0.0`；分支 CI `33840660399` 成功。
- 2026-09-04：`maintenance-1-v1.1.1` 精确指向 `fc3702f`；Release `33840769312` / 作业 `100922429982` 成功完成 npm publish。
- 2026-09-04：npm 官方源确认 `1.1.1`、`maintenance-1=1.1.1`、`latest=0.1.59`；精确版本临时执行、login help、主 Skill、update Skill 与本机 Codex Skill 刷新通过，进入审计回填。
- 2026-09-04：发布审计提交 `e4c5631` 普通推送到 `origin/1.0.0`，分支 CI `33841934200` 成功；全部验收项通过，任务完成并归档。
