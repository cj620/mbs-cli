# 建立 1.0.0 长期维护分支执行计划

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 建立 1.0.0 长期维护分支 |
| 任务级别 | L3（公共版本、长期发布线、远程分支写入） |
| 任务类型 | RELEASE |
| 任务状态 | 待验收 |
| 当前阶段 | Git 分支已交付，等待远程 CI 与后续 npm 发布决策 |
| 交付状态 | 待发布 |
| 执行授权 | 已授权 |
| 授权来源 | 用户明确要求“你帮我执行”，承接此前“将当前分支版本改为1.0.0推送到1.0.0分支，作为长期维护不与master重合” |
| 授权范围 | 分支改名、CLI 版本、长期分支 CI、必要审计文档、本地验证、精确提交并推送 `origin/1.0.0`；不含 npm 发布、标签、旧分支删除、PR 和 master 写入 |
| 计划完成后动作 | 推送并核验远程分支后停止，等待 npm 发布的独立授权与验收 |
| 验证级别 | V3 |
| 创建时间 | 2026-08-11 |
| 最后更新时间 | 2026-08-11 |
| 需求文档 | [`./需求描述.md`](./需求描述.md) |
| 仓库与分支 | `D:\workspace\mbs-cli` / `1.0.0`，上游 `origin/1.0.0` |
| 基线 | `a8a0bdbc7e22488c181b564d84f98cefcba62ecc` |
| 执行环境 | Windows / PowerShell / Node 20 项目基线 / pnpm workspace |

## 恢复执行入口

- **任务目标**：把当前功能分支建立为独立的 `1.0.0` 长期维护分支并推送，不触碰 master。
- **当前进度**：本地和远程 `1.0.0` 已建立，初始交付提交为 `0c61a72bece8b3d613ee8a48bfb1a151eda280bd`；旧功能分支和 origin/master 哈希保持不变；V3 本地验证通过。
- **下一步**：观察 GitHub CI；npm 发布需另行确认标签、dist-tag 和自动更新通道后执行。
- **阻塞**：无；GitHub CLI 未安装，但本任务不创建 PR，原生 Git 可完成已授权推送。
- **待确认**：npm 发布通道、标签格式和自动更新通道不在本任务范围。
- **未验证**：GitHub CI 实际结果和目标环境业务链路；npm 发布不在本任务范围。
- **重要警告**：不得运行现有 `pnpm release:*`，该脚本只允许 master 并会固定推送到 master；不得创建发布标签。
- **执行授权**：已授权。
- **当前允许操作**：任务范围文件修改、本地分支改名、本地验证、精确暂存提交、推送新远程分支。
- **当前禁止操作**：合并或修改 master、删除旧远程分支、强制推送、创建标签、发布 npm、修改 dist-tag、创建 PR。
- **授权方式**：用户已明确要求当前执行者执行。
- **授权后的第一步**：已核对仓库、工作区、当前基线、远程和目标引用。

## 已确认决策、执行假设与约束

### 已确认决策

| 编号 | 决策 | 原因 | 来源 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| D-001 | 当前分支改名为 `1.0.0` 并独立长期维护 | 避免与 master 的两套 manifest 和插件架构强行合并 | 用户决定 | 有效 |
| D-002 | 本任务只推送 Git 分支，不发布 npm | npm 发布会改变外部安装与更新通道，需要独立验收和授权 | 用户范围与安全边界 | 有效 |
| D-003 | 保留旧远程分支 | 远程删除不可由新分支推送授权自动推导，且保留恢复入口 | Git 安全规则 | 有效 |

### 执行假设

| 编号 | 假设 | 依据 | 影响 | 验证与错误处理 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| A-001 | CLI 自身版本变化无需更新 `pnpm-lock.yaml` | 现有发布脚本只修改 `packages/cli/package.json` | 锁文件应保持不变 | 提交前检查锁文件差异；出现变化时调查而非自动接受 | 待验证 |

### 强制约束

- 不修改业务行为、manifest、认证、安全或数据边界。
- 不覆盖、删除或强制更新任何现有远程引用。
- 推送前必须完成 V3 本地检查并逐项审查暂存差异。
- 任务文档不得记录凭据、真实用户数据和完整外部响应。

## 影响范围

### 预计修改文件

- `packages/cli/package.json`
- `.github/workflows/ci.yml`
- `.docs/req_doc/README.md`
- `.docs/memory/README.md`
- `.docs/memory/topics/1.0.0长期维护分支.md`
- 当前任务的 `需求描述.md` 和 `执行计划.md`

### 明确不修改

- master、origin/master 和旧远程功能分支。
- `.github/workflows/release.yml`、发布脚本和 npm 更新通道。
- 业务代码、生成 manifest、Skill 内容和 workspace 依赖。

## 执行步骤

### S1：建立审计和长期记忆

- **状态**：已完成
- **目标**：记录长期分支的范围、授权、恢复入口和不合并决策。
- **验证方式**：检查索引、链接、状态和敏感信息。

### S2：改名分支并修改版本与 CI

- **状态**：已完成
- **目标**：建立本地 `1.0.0` 分支身份和最小维护配置。
- **验证方式**：Git 分支、JSON 解析、YAML 差异和范围审查。

### S3：执行 V3 本地验证

- **状态**：已完成
- **目标**：验证版本和 CI 配置未破坏构建、测试、发布保护与主要命令发现。
- **验证方式**：`pnpm build`、`pnpm test`、发布保护测试及代表性 `--help`。
- **实际执行结果**：frozen install 与构建通过；串行 workspace 测试和原始 `pnpm test` 复验通过，共 158 条测试；发布工作流、发布脚本和 bundle 保护共 13 条检查通过；CLI 显示 `1.0.0`，6 条帮助命令退出 0。
- **验证证据**：首次并行 `pnpm test` 中 legacy 的 10 条断言均通过但进程异常返回 1；定向 legacy、串行 workspace 和一次原始并行复验均通过，未修改代码掩盖该次异常。

### S4：提交并推送远程分支

- **状态**：已完成
- **目标**：仅提交任务范围文件并创建 `origin/1.0.0`。
- **验证方式**：暂存差异、提交对象、远程引用、上游跟踪和最终工作区状态。
- **实际执行结果**：提交 `0c61a72` 已通过普通非强制推送创建 `origin/1.0.0`，本地分支已跟踪该上游；旧远程功能分支仍为 `a8a0bdb`，origin/master 仍为 `be8c047`。

## 验收检查

| 编号 | 验收项 | 级别 | 方式 | 预期 | 实际 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| AC-01 | 分支与版本 | V3 | Git 与 JSON 检查 | 本地/远程分支为 `1.0.0`，CLI 版本为 `1.0.0` | 本地和远程分支、版本均通过 | 通过 |
| AC-02 | CI 覆盖 | V3 | YAML 差异与保护测试 | master 与 `1.0.0` 均被覆盖 | 配置已覆盖两分支，发布保护测试通过 | 通过 |
| AC-03 | 构建测试 | V3 | 全 workspace 构建测试 | 全部通过 | frozen install、构建、158 条全量测试和 13 条发布保护检查通过 | 通过 |
| AC-04 | 命令发现 | V3 | 代表性帮助命令 | 全部退出 0 | CLI 显示 1.0.0，6 条帮助命令退出 0 | 通过 |
| AC-05 | 远程隔离 | V3 | 远程引用核验 | 只新增 `origin/1.0.0`，其他引用不变 | 初始交付提交为 `0c61a72`；旧分支和 master 哈希未变 | 通过 |

## 风险与回滚

| 风险 | 影响 | 控制 | 回滚 |
| :--- | :--- | :--- | :--- |
| 固定版本名分支后续维护语义不清 | 发布 1.0.1 后分支名仍为 1.0.0 | 文档明确它是长期线，不自动改名 | 后续经用户授权迁移为 `release/1.x` |
| 误触 master 发布脚本 | 覆盖 master 或创建错误标签 | 禁止运行 `pnpm release:*`，只执行精确 Git 命令 | 推送前核对 refspec；未推送前停止并恢复本地分支名 |
| 分支推送后 CI 失败 | 远程存在未通过分支 | 本地先完成 V3；CI 失败时保留分支并修复，不删除或强推 | 新增修复提交；必要时停止使用该分支 |
| 版本 1.0.0 被误认为 npm 已发布 | 用户安装预期错误 | 文档和交付明确“仅 Git 分支，npm 未发布” | 完成独立发布流程前不创建标签 |

## 上线前验证与观察

- 本任务只交付 Git 分支，不构成 npm 发布或生产上线。
- npm 发布前必须另行隔离 dist-tag、更新通道和标签来源，并完成目标环境只读链路验收。
- 远程分支推送后检查 GitHub CI；无法在当前会话取得 CI 结果时保持“待验收/未发布”。

## Git、依赖、版本与规则记录

- 当前远程：`https://github.com/cj620/mbs-cli.git`。
- 目标操作：本地分支改名、精确提交、普通推送并设置上游；不强推、不删分支、不创建 PR 或标签。
- 不新增依赖，不修改锁文件。
- 已读取 R-01 至 R-10 的当前任务适用模块及 `github:yeet` 技能；技能的 PR 环节因用户明确不与 master 合并而不适用，且本机未安装 `gh`，提交推送使用原生 Git。
- `.docs/项目规范.md` 不存在；遵守根 `AGENTS.md`、现有任务记忆和代码事实。

## 变更记录

| 时间 | 变更 | 原因 |
| :--- | :--- | :--- |
| 2026-08-11 | 创建 L3 需求、计划和恢复入口 | 用户授权建立并推送独立 1.0.0 长期维护分支 |
| 2026-08-11 | 完成本地分支改名、版本/CI 修改与 V3 本地验证 | 提交推送前证明维护分支可构建、测试且发布保护未回归 |
| 2026-08-11 | 创建提交 `0c61a72` 并推送 `origin/1.0.0` | 完成用户授权的独立长期 Git 分支交付，保留旧分支和 master |
