# 修复语义召回服务路由执行计划

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 修复语义召回服务路由并发布 1.0.1 |
| 任务级别 | L3（公共请求契约、远程分支与生产 npm 发布） |
| 任务类型 | BUG / RELEASE |
| 任务状态 | 执行中 |
| 当前阶段 | 提交与远程分支 CI |
| 交付状态 | 未发布 |
| 执行授权 | 已授权 |
| 授权来源 | 用户确认可直接覆盖 1.0.0 的代码并明确要求“同意并且提交代码发布npm” |
| 授权范围 | 修复 find/describe 的 cli-service 路由、升级为 1.0.1、V3 验证、提交并推送 `origin/1.0.0`、创建并推送 `maintenance-1-v1.0.1`、发布 npm `maintenance-1`；不含 master、latest、强推、PR 或 1.0.0 弃用 |
| 验证级别 | V3 |
| 创建时间 | 2026-08-12 |
| 最后更新时间 | 2026-08-12 |
| 需求文档 | [`./需求描述.md`](./需求描述.md) |
| 仓库与分支 | `D:\workspace\mbs-cli` / `1.0.0`，上游 `origin/1.0.0` |
| 基线 | `a30be6a5bd3b8997be9e0af79e28ff9dd44a95cb` |

## 恢复执行入口

- **任务目标**：恢复语义召回的 `cli-service` 服务层，并将修复作为 `1.0.1` 发布到维护通道。
- **当前进度**：路由与版本修复完成；frozen install、构建、158 条全量测试、13 条发布保护和代表性 CLI 命令均通过。
- **下一步**：完成提交范围审查，提交并推送 `origin/1.0.0`，等待分支 CI。
- **阻塞**：无。
- **待确认**：无；用户已授权提交与 npm 发布，既有维护发布契约要求使用 `maintenance-1` 且不移动 `latest`。
- **未验证**：远程 CI、npm 发布和官方源安装尚未执行。
- **重要警告**：只有分支 CI 成功后才能创建 `maintenance-1-v1.0.1`；发布前后都必须核验 `latest=0.1.58`。
- **当前允许操作**：任务范围内代码、测试、版本、审计与记忆修改；普通提交和推送；门禁通过后的精确维护标签推送与 npm 发布观察。
- **当前禁止操作**：修改或合并 master、更新 npm latest、强制推送、删除远程引用、创建 PR、弃用 1.0.0、泄露认证或生产响应。

## 已确认决策与约束

| 编号 | 决策 | 依据 | 状态 |
| :--- | :--- | :--- | :--- |
| D-001 | 正确完整召回地址为 `/cli/cli-service/cli/api/recall` | 用户确认与后端控制器/服务名核验 | 有效 |
| D-002 | `describe` 同步使用 `/cli/cli-service/cli/api/recall/detail` | 共用召回客户端与同一控制器契约 | 有效 |
| D-003 | 已发布 1.0.0 不覆盖，使用新补丁版本 1.0.1 | npm 版本不可重复发布 | 有效 |
| D-004 | 发布到 `maintenance-1`，不更新 `latest` | 用户此前选择且当前维护工作流契约 | 有效 |

- CLI 继续只发查询类 POST，不改变数据或权限边界。
- 不新增依赖，不直接发布带 `workspace:*` 的源包，继续使用现有 deploy bundle。
- 任何核心验证或远程门禁失败都停止后续发布动作，使用新增修复提交而非历史改写。

## 执行步骤

### S1：建立审计并冻结基线

- **状态**：已完成
- **目标**：记录范围、授权、风险、恢复入口和发布边界。
- **验证**：索引可达、工作区基线和远程/npm 状态可复核。

### S2：补充失败回归测试

- **状态**：已完成
- **目标**：直接覆盖 find 与 describe 的最终完整请求地址。
- **通过标准**：修改生产代码前，新增断言因实际 `/gateway/cli/...` 与预期 `/cli/cli-service/...` 不同而稳定失败。
- **实际结果**：新增完整 find/detail URL 断言；生产代码修改前定向运行 13 条测试，12 条通过、1 条按预期失败，失败值为 `/gateway/cli/cli/api/recall`。

### S3：修复路由并升级版本

- **状态**：已完成
- **目标**：最小修改召回客户端基础路径，并将 CLI 版本升级为 `1.0.1`。
- **通过标准**：定向测试通过，差异不改变相对路径、认证、请求体或响应协议。
- **实际结果**：仅将召回客户端基础前缀改为 `/cli/cli-service` 并升级 CLI 至 `1.0.1`；find 与认证两组共 14 条定向测试通过。

### S4：执行 V3 本地验证

- **状态**：已完成
- **验证**：frozen install、全量构建测试、发布保护、代表性 CLI 命令、差异/文档/敏感信息检查。
- **失败处理**：停止提交或发布，保留证据并修复。
- **实际结果**：`pnpm install --frozen-lockfile`、`pnpm build`、`pnpm test` 全部通过；脚本 12 条加 workspace 146 条共 158 条测试通过；13 条发布工作流/脚本/bundle 保护通过；CLI 显示 `1.0.1`，find/describe 帮助退出 0；锁文件未变化。

### S5：提交推送并等待分支 CI

- **状态**：待执行
- **目标**：提交任务范围变更并普通推送 `origin/1.0.0`。
- **通过标准**：远程分支指向提交且 GitHub CI 成功。

### S6：发布并验证 npm maintenance-1

- **状态**：待执行
- **目标**：推送 `maintenance-1-v1.0.1`，由 Release 工作流发布 `1.0.1`。
- **通过标准**：Release 成功；`maintenance-1=1.0.1`、`latest=0.1.58`；官方源包可独立运行。
- **失败处理**：停止额外远程写入；不得通过移动 latest 补救。

### S7：回填审计与项目记忆

- **状态**：待执行
- **目标**：记录验证证据、提交/运行标识、发布事实和剩余风险，再提交推送最终记录。

## 风险与回滚

| 风险 | 控制 | 回滚/处置 |
| :--- | :--- | :--- |
| 路由再次遗漏服务层 | 断言 find 和 describe 的完整路径 | 新增普通修复提交恢复正确前缀 |
| 修复意外影响普通用户 | 只发布 `maintenance-1`，发布前后读取 dist-tags | 若标签异常，停止并请求独立 dist-tag 修复授权 |
| npm 1.0.1 已占用 | 发布前查精确版本 | 改用下一未占用补丁版本前需重新记录决策 |
| CI 或发布失败 | 本地 V3 + 分支 CI 两道门禁 | 保留失败运行，新提交修复，不强推或删除证据 |

## 变更记录

| 时间 | 变更 | 原因 |
| :--- | :--- | :--- |
| 2026-08-12 | 创建 L3 需求、计划与恢复入口 | 用户授权修复、提交并发布 npm 维护版本 |
| 2026-08-12 | 完成失败回归、最小路由修复和 1.0.1 版本升级 | 证明旧路径缺少 cli-service，并在同一测试缝隙确认修复生效 |
| 2026-08-12 | 完成 V3 本地核心门禁和项目记忆更新 | 提交前确认构建、测试、发布保护、命令发现和长期事实一致 |
