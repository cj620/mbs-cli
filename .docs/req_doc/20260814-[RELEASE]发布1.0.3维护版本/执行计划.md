# 发布 1.0.3 维护版本执行计划

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 发布 1.0.3 维护版本 |
| 任务级别 | L3（公共请求契约、远程分支与生产 npm 发布） |
| 任务类型 | RELEASE |
| 任务状态 | 已完成 |
| 当前阶段 | 发布后核验与审计回填完成 |
| 交付状态 | 已发布 |
| 执行授权 | 已授权 |
| 授权来源 | 用户明确要求“提交代码并发布1.0.3” |
| 授权范围 | 升级 1.0.3、提交并推送 `origin/1.0.0`、创建并推送 `maintenance-1-v1.0.3`、发布 npm `maintenance-1`；不含 master、latest、强推、删除引用或覆盖旧版本 |
| 验证级别 | V3 |
| 创建和更新时间 | 2026-08-14 |
| 需求文档 | [`./需求描述.md`](./需求描述.md) |
| 仓库与分支 | `D:\workspace\mbs-cli` / `1.0.0`，上游 `origin/1.0.0` |
| 基线 | `ae56c93bfa49903a84ec293afbe4ab534d5fc82c` |

## 恢复执行入口

- **任务目标**：把 CLI 网关路由顺序修复作为 `1.0.3` 发布到 npm `maintenance-1`，不影响普通安装用户。
- **当前进度**：本地 V3、维护分支推送、分支 CI、维护标签、Release、npm dist-tag 和官方源安装核验全部完成。
- **下一步**：无；后续维护修复必须使用新的未发布补丁版本并重新取得发布授权。
- **阻塞**：无。
- **未验证**：真实登录态下的生产业务查询不纳入发布门禁；不得输出或保存用户凭据与生产响应。
- **重要警告**：`1.0.3` 已发布且不可覆盖；不得移动 `latest`，后续修改不得重用本次标签或版本。

## 已确认决策与约束

| 编号 | 决策 | 依据 | 状态 |
| :--- | :--- | :--- | :--- |
| D-001 | 新版本使用 `1.0.3` | 用户明确指定；Git 标签和 npm 精确版本均未占用 | 有效 |
| D-002 | 只发布到 `maintenance-1` | 长期维护线既有发布契约 | 有效 |
| D-003 | `latest` 必须保持 `0.1.58` | 用户要求不影响普通安装用户 | 有效 |
| D-004 | 发布标签必须指向通过 CI 的维护分支提交 | Release 工作流来源分支门禁 | 有效 |
| D-005 | `.idea/claudeCodeTabState.xml` 不进入任何提交 | 文件为用户本地修改 | 有效 |

## 执行步骤

### S1：冻结发布范围与远端状态

- **状态**：已完成
- **实际结果**：本地与 `origin/1.0.0` 分歧为 `0/0`；`maintenance-1-v1.0.3` 本地和远端均不存在；npm `1.0.3` 未发布；发布前 dist-tag 为 `maintenance-1=1.0.2`、`latest=0.1.58`。

### S2：升级版本并执行本地 V3

- **状态**：已完成
- **验证**：`pnpm install --frozen-lockfile`、生成 dry-run、`pnpm build`、`pnpm test`、发布保护、代表性 CLI 帮助、完整 URL、差异与敏感信息检查。
- **实际结果**：冻结安装与生成 dry-run 通过，manifest hash 为 `2b24bbad...d84df`；14 个 workspace 范围构建通过；脚本 12 条加 workspace 166 条共 178 条测试通过；13 条发布工作流、脚本和 bundle 保护通过；CLI 显示 `1.0.3`，find/describe/request/login/global Skill 入口退出 0；源 manifest 中 `/gateway/` action 数量为 0，find/describe 编译产物为 `/gateway/cli/cli-service`；锁文件无变化且 `git diff --check` 通过。

### S3：精确提交并推送维护分支

- **状态**：已完成
- **通过标准**：只提交任务范围文件；用户 `.idea` 修改不进入暂存；`origin/1.0.0` 包含发布提交；GitHub 分支 CI 成功。
- **实际结果**：发布提交 `ee9c017783dedf5730b272306be383bedc3bffee` 已普通推送；用户 `.idea` 修改未提交；分支 CI `31765647873` 成功。

### S4：创建维护标签并发布 npm

- **状态**：已完成
- **通过标准**：`maintenance-1-v1.0.3` 指向发布提交；Release 成功；npm 精确版本与 dist-tag 正确。
- **实际结果**：轻量标签 `maintenance-1-v1.0.3` 指向 `ee9c017`；Release `31765742742` 成功；npm 官方源为 `maintenance-1=1.0.3`、`latest=0.1.58`。

### S5：核验官方源产物并回填审计

- **状态**：已完成
- **通过标准**：官方源安装包版本、命令入口、find/describe 及生成业务路由通过；记录提交、CI、Release、标签和 dist-tag 证据并推送纯文档提交。
- **实际结果**：隔离临时目录从 npm 官方源安装精确版本后，`mbs --version` 输出 `1.0.3`，find/describe/request/login 帮助通过；编译产物包含 `/gateway/cli/cli-service` 且不含旧 `/cli/gateway/cli-service`，13 个受影响业务命令均使用相对 service 路径；临时目录已清理，未读取登录态或发送业务请求。

## 验收检查

| 编号 | 验收项 | 实际 | 状态 |
| :--- | :--- | :--- | :--- |
| AC-01 | 本地 V3 | frozen install、生成、构建、178 条测试、13 条发布保护和代表性命令通过 | 通过 |
| AC-02 | 维护分支门禁 | `ee9c017` 属于 `origin/1.0.0`，CI `31765647873` 成功 | 通过 |
| AC-03 | 维护发布 | 标签指向正确提交，Release `31765742742` 成功 | 通过 |
| AC-04 | 通道隔离 | `maintenance-1=1.0.3`，`latest=0.1.58` | 通过 |
| AC-05 | 官方源产物 | 版本、4 个帮助入口、召回前缀和 13 条生成业务路由核验通过 | 通过 |

## 风险与处置

| 风险 | 控制 | 失败处置 |
| :--- | :--- | :--- |
| 意外影响普通安装用户 | 标签前后读取 npm dist-tags | 立即停止，不通过移动 latest 补救 |
| 大量生成文件掩盖额外语义变化 | 屏蔽 manifest hash 后复核语义差异并执行生成 dry-run | 停止发布并调查漂移 |
| 分支或发布工作流失败 | 本地 V3、分支 CI、Release 三道门禁 | 保留失败证据，修复后使用普通追加提交 |
| 混入用户 IDE 状态 | 显式排除 `.idea` 并检查 staged diff | 提交前撤出暂存，不覆盖用户文件 |

## 变更记录

| 时间 | 变更 | 原因 |
| :--- | :--- | :--- |
| 2026-08-14 | 创建发布需求、计划并选择 1.0.3 | 用户授权提交发布；确认指定版本与标签未占用 |
| 2026-08-14 | 完成版本升级与本地 V3 | 在任何远端写入前验证生成、构建、178 条测试、13 条发布保护、命令入口和完整路由 |
| 2026-08-14 | 推送 `ee9c017`，分支 CI `31765647873` 成功 | 通过维护分支门禁后才创建发布标签 |
| 2026-08-14 | 推送 `maintenance-1-v1.0.3`，Release `31765742742` 成功 | 发布到维护通道且未移动 latest |
| 2026-08-14 | 完成 npm dist-tag 和官方源安装核验 | 确认实际 registry 产物、15 条路由与用户安装入口 |
