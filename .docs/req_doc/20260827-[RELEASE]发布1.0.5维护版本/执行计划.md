# 发布 1.0.5 维护版本执行计划

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 发布 1.0.5 维护版本 |
| 任务级别 | L3（公共输出契约、远程分支与生产 npm 发布） |
| 任务类型 | RELEASE |
| 任务状态 | 执行中 |
| 当前阶段 | 本地 V3 通过，准备精确提交与推送维护分支 |
| 交付状态 | 未交付 |
| 执行授权 | 已授权 |
| 授权来源 | 用户明确要求“发布1.0.5” |
| 授权范围 | 升级 1.0.5、精确提交并普通推送 `origin/1.0.0`、创建并推送 `maintenance-1-v1.0.5`、通过既有工作流发布 npm `maintenance-1`；不含 master、latest、强推、删除引用、覆盖旧版本或真实业务请求 |
| 计划完成后动作 | 当前范围内继续实施直至发布后核验与审计回填完成 |
| 验证级别 | V3 |
| 创建和更新时间 | 2026-08-27 |
| 需求文档 | [`./需求描述.md`](./需求描述.md) |
| 仓库与分支 | `D:\workspace\mbs-cli` / `1.0.0`，上游 `origin/1.0.0` |
| 基线 | `aef35041a63fd20a44fbdd44784a3010e8b091bd` |
| 执行环境 | Windows / PowerShell / Node 20 兼容项目工具链 / pnpm 10.28.2 |

## 恢复执行入口

- **任务目标**：把后端响应原样透传作为 `1.0.5` 发布到 npm `maintenance-1`，不移动普通 `latest`。
- **当前进度**：已确认 npm `1.0.5` 与远端维护标签未占用；版本升级、frozen install、manifest dry-run、14 个工作区构建、201 条测试、13 条发布保护、CLI 版本/帮助和编译产物透传 smoke 已通过。
- **下一步**：精确暂存并复核 staged diff，提交后普通推送维护分支并等待 CI。
- **阻塞**：无。
- **待确认**：无；用户已明确指定 1.0.5、提交和发布。
- **未验证**：分支 CI、Release、npm 官方源产物、真实目标网关和下游迁移。
- **重要警告**：不得提交用户 `.idea/claudeCodeTabState.xml`；不得移动 `latest`；标签只能在分支 CI 成功后创建。
- **执行授权**：已授权。
- **当前允许操作**：任务范围修改、验证、精确提交、普通推送维护分支；CI 成功后创建并推送维护标签及核验发布。
- **当前禁止操作**：修改/推送 master、移动 latest、强推、删除或覆盖引用、覆盖已发布 npm 版本、真实业务请求、读取或记录敏感信息。

## 已确认决策、执行假设与约束

| 编号 | 类型 | 内容 | 依据 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| D-001 | 决策 | 使用未占用的 `1.0.5` 与 `maintenance-1-v1.0.5` | 用户在确认 1.0.4 已占用后明确指定 | 有效 |
| D-002 | 决策 | 只发布到 `maintenance-1` | `1.0.0` 长期维护线既有契约 | 有效 |
| D-003 | 决策 | 发布前 `latest=0.1.59`，发布后必须保持不变 | npm 官方源发布前核验 | 有效 |
| D-004 | 决策 | 标签只指向通过 CI 的维护分支提交 | Release 工作流来源分支门禁 | 有效 |
| D-005 | 决策 | `.idea/claudeCodeTabState.xml` 不进入任何提交 | 既有用户本地修改 | 有效 |
| A-001 | 假设 | 不发送真实目标网关业务请求 | 使用自动化、编译产物和隔离安装的人工最小 mock 验证公共输出契约 | 有效 |

## 影响范围

- **预计修改**：`packages/cli/package.json`、后端响应透传功能及其既有工作区差异、测试、README/Skill/AGENTS、记忆/审计和本发布任务文档。
- **明确不修改**：`.idea/claudeCodeTabState.xml` 内容、master、latest、认证配置、生产数据与外部业务系统。
- **关联参考**：[`../20260827-[FEATURE]透传后端响应内容/`](../20260827-[FEATURE]透传后端响应内容/)、[`../../memory/topics/1.0.0长期维护分支.md`](../../memory/topics/1.0.0长期维护分支.md)、[`../../memory/topics/CLI后端响应透传.md`](../../memory/topics/CLI后端响应透传.md)。

## 执行步骤

### S1：冻结发布范围与远端状态

- **状态**：已完成
- **实际结果**：本地与远端 `1.0.0` 均为 `aef3504`；npm `1.0.5` 与远端 `maintenance-1-v1.0.5` 未占用；npm 为 `maintenance-1=1.0.4`、`latest=0.1.59`；`.idea` 明确排除。

### S2：升级版本并执行本地 V3

- **状态**：已完成
- **操作**：升级 CLI 版本；执行 frozen install、manifest dry-run、全量构建与测试、发布保护、编译产物 smoke、差异和敏感信息检查。
- **通过标准**：全部核心检查通过，暂存差异只含发布范围，未发现凭据或未说明失败。
- **失败处理**：停止远程写入；记录失败并在范围内修复或保持阻塞。
- **实际结果**：`pnpm install --frozen-lockfile` 通过；manifest dry-run hash 为 `bfab8e9d...fa76953`；14 个工作区构建、201 条测试、发布工作流 1 项、发布脚本 7 条、bundle 5 条共 13 条保护通过；CLI 显示 `1.0.5`，`request --help` 正常；编译后的成功 body、后端错误 body 和错误退出码 smoke 通过；`git diff --check` 与定向敏感模式检查通过。

### S3：精确提交并推送维护分支

- **状态**：待执行
- **前置条件**：S2 完成。
- **操作**：逐路径暂存和复核 staged diff；提交；普通推送 `HEAD:1.0.0`；观察对应 GitHub Actions CI。
- **失败处理**：不强推、不覆盖远端；保留本地提交并调查。

### S4：创建维护标签并发布 npm

- **状态**：待执行
- **前置条件**：S3 的维护分支 CI 成功。
- **操作**：创建轻量标签 `maintenance-1-v1.0.5` 并普通推送；观察 Release 工作流。
- **失败处理**：不移动或覆盖标签；使用普通追加提交和新的未发布版本恢复。

### S5：核验官方源产物并回填审计

- **状态**：待执行
- **操作**：从 npm 官方源隔离安装精确版本，核验版本与透传契约；更新功能任务、记忆、索引和本发布记录并提交推送纯文档回填。
- **失败处理**：明确 npm 已发布但产物核验失败；不得覆盖 1.0.5。

## 验收检查

| 编号 | 验收项 | 当前状态 |
| :--- | :--- | :--- |
| AC-01 | 本地 V3 与发布保护 | 通过 |
| AC-02 | 提交范围与 `.idea` 隔离 | 待执行 |
| AC-03 | 维护分支提交、推送与 CI | 待执行 |
| AC-04 | 维护标签与 Release | 待执行 |
| AC-05 | `maintenance-1=1.0.5` 且 `latest=0.1.59` | 待执行 |
| AC-06 | 官方源隔离安装和透传产物 | 待执行 |

## 风险、数据保护与回滚

| 风险 | 控制 | 回滚或失败处置 |
| :--- | :--- | :--- |
| 下游仍解析旧 envelope | breaking contract 文档、自动化和产物 smoke | 发布后以新补丁普通反向提交恢复，不覆盖 1.0.5 |
| 后端错误 body 含敏感信息 | CLI 不记录 body；本次只用人工最小 mock | 停止扩大发布，由后端修正客户响应契约 |
| 意外影响普通安装用户 | 标签前后核对 dist-tags，只使用维护标签 | 立即停止，不移动 latest |
| 混入用户 IDE 状态 | 逐路径暂存并复核 staged diff | 提交前停止；不覆盖用户文件 |
| 分支或工作流失败 | 本地 V3、分支 CI、Release 三道门禁 | 保留证据，普通追加提交；禁止强推和复用版本 |

## Git、依赖、安全与规则记录

- 仓库：`D:\workspace\mbs-cli`；分支：`1.0.0`；基线：`aef3504`；远端目标：`origin/1.0.0`。
- 计划 Git 操作：精确暂存、提交、普通推送维护分支；CI 成功后创建并推送一个新轻量标签；不创建 PR，不操作 master。
- 本次不新增或升级依赖；使用现有 `pnpm-lock.yaml` 与项目固定 pnpm 版本。
- 不读取登录态、环境变量值、Cookie 或真实请求/响应；扫描只输出文件或检查结论。
- 已完整读取 R-01 至 R-08、R-10；使用 GitHub 技能观察分支 CI 与 Release。

## 变更记录

| 时间 | 变更 | 原因 |
| :--- | :--- | :--- |
| 2026-08-27 | 创建发布需求和计划，选择 1.0.5 | npm 1.0.4 已发布不可覆盖；用户明确改发 1.0.5 |
| 2026-08-27 | 完成版本升级与本地 V3 | 在远程写入前完成 frozen install、生成、构建、201 条测试、13 条发布保护和编译产物 smoke |
