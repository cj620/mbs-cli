# 修正 CLI 网关路由顺序执行计划

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 修正 CLI 网关路由顺序 |
| 任务级别 | L3（已发布公共请求契约、跨多个生成业务包） |
| 任务类型 | BUG |
| 任务状态 | 已完成 |
| 当前阶段 | 已纳入 1.0.3 发布范围 |
| 交付状态 | 待发布 |
| 执行授权 | 已授权 |
| 授权来源 | 用户明确要求“find / describe 地址修改为正确的，13条 manifest 路径应当改为…” |
| 授权范围 | 修正并验证 find/describe 和 13 条 manifest 路径；解释但不修改 serve |
| 计划完成后动作 | 由 `20260814-[RELEASE]发布1.0.3维护版本` 完成提交、推送与维护通道发布 |
| 验证级别 | V3 |
| 创建和更新时间 | 2026-08-14 |
| 需求文档 | [`./需求描述.md`](./需求描述.md) |
| 分支与基线 | `D:\workspace\mbs-cli` / `1.0.0` / `ae56c93` |
| 执行环境 | Windows / PowerShell / pnpm workspace |

## 恢复执行入口

- **任务目标**：统一请求地址为 `<apiUrl>/gateway/cli/<service>/...`。
- **当前进度**：召回前缀与 13 条源 manifest 路径已修正，生成产物已更新；定向测试、全量 178 条测试、全 workspace 构建及 15 个完整 URL 核验通过，用户已授权发布 `1.0.3`。
- **下一步**：由关联发布任务执行版本门禁、维护分支 CI、标签发布和 npm 官方源核验。
- **阻塞**：无。
- **待确认**：`serve --project-apis` 是否也应统一使用 `/gateway/cli`；本任务只解释现状，不修改。
- **未验证**：真实服务请求、serve 上游对齐、Git/CI、安装包和 npm 发布。
- **重要警告**：`.idea/claudeCodeTabState.xml` 是用户修改，不得触碰；不得记录或发送 Cookie。
- **执行授权**：已授权。
- **当前允许操作**：关联发布任务范围内的版本升级、精确暂存、普通提交与推送、维护标签和 npm `maintenance-1` 发布。
- **当前禁止操作**：修改或推送 master、移动 npm `latest`、强推、删除引用、真实业务请求及 serve 行为修改。

## 已确认决策、执行假设与约束

| 编号 | 决策 | 来源 | 状态 |
| :--- | :--- | :--- | :--- |
| D-001 | find/describe 使用 `/gateway/cli/cli-service/cli/api/recall...` | 用户当前确认 | 有效 |
| D-002 | manifest action path 相对于 `/gateway/cli`，13 条移除开头 `/gateway` | 用户当前确认 | 有效 |
| D-003 | 本轮不修改 serve，只解释其上游基础前缀差异 | 用户当前问题与最小授权边界 | 有效 |

### 规则冲突

| 编号 | 规则一 | 规则二 | 冲突内容 | 影响范围 | 当前处理 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| F-001 | 用户当前确认 | npm `1.0.2` 审计与项目记忆 | 路由顺序分别为 `/gateway/cli` 与 `/cli/gateway` | find/describe、生成业务命令、记忆 | 按当前明确决定建立部分修正任务；已发布事实保留，待发布变化单独记录 | 已解决 |

- HTTP 请求继续只允许 GET 和查询类 POST。
- 不新增依赖，不改变认证、权限和错误契约。
- 自动生成文件只通过源 manifest 和生成器更新。

## 影响范围

### 预计修改

- `packages/cli/src/find/remote-client.ts`
- `packages/cli/src/__tests__/find.test.ts`
- `packages/cli/src/__tests__/remote-client-auth.test.ts`
- `packages/cli/src/__tests__/serve.test.ts`
- `manifests/mbs-api-manifest.json`
- manifest 生成器产生的业务命令、CLI 注册、项目 manifest 和 Skill 文档
- 当前任务审计索引与相关项目记忆

### 明确不修改

- `.idea/claudeCodeTabState.xml` 和其他用户修改。
- 登录、Cookie、刷新、响应及权限逻辑。
- `serve` 的客户端基础地址和 `/proxy/*` 语义。
- 锁文件和既有 Git 历史；版本与远端新增提交由关联发布任务负责。

## 执行前提

- 当前分支为 `1.0.0`，工作区仅有一项不相关 `.idea` 用户修改。
- 已安装依赖可完成本地生成、测试和构建。
- 验证使用人工构造 URL，不访问真实服务。

## 执行步骤

### S1：建立失败回归

- **状态**：已完成
- **目标**：让错误路由顺序和 manifest 重复网关前缀在当前代码上稳定失败。
- **通过标准**：失败内容精确显示预期 `/gateway/cli` 与实际 `/cli/gateway`，并检出 13 条 `/gateway/` action。
- **证据**：3 个测试文件共 37 条中，修复前 3 条按预期失败，其余 34 条通过；失败分别显示 URL 顺序差异及完整 13 条异常 action。

### S2：最小修复与重新生成

- **状态**：已完成
- **目标**：修正召回常量和源 manifest，运行生成器更新禁止手工维护的产物。
- **通过标准**：S1 全部转绿，生成 dry-run 可完整解析源 manifest。
- **证据**：37 条定向测试全部通过；生成器成功生成新 hash `2b24bbad...d84df`，dry-run 退出码 0。因整份 manifest hash 写入所有生成文件头，共 1562 个 package/Skill 文件变化；屏蔽 hash 后只有 18 个文件含语义变化，其中 4 个为手工代码/测试、1 个项目 manifest、13 个生成命令。

### S3：V3 验证与记忆同步

- **状态**：已完成
- **目标**：完成相关回归、全量测试、构建、路径清单和差异审查，记录本地待发布事实。
- **证据**：`pnpm test` 共 178 条通过，`pnpm build` 14 个 workspace 范围全部通过；地址脚本确认 find、describe 和 13 条业务路径，且源 manifest 中 `/gateway/` action 数量为 0；`git diff --check` 退出码 0。

## 验收检查

| 编号 | 验收项 | 预期 | 状态 |
| :--- | :--- | :--- | :--- |
| AC-01 | find 完整 URL | `<root>/gateway/cli/cli-service/cli/api/recall` | 通过 |
| AC-02 | describe 完整 URL | `<root>/gateway/cli/cli-service/cli/api/recall/detail` | 通过 |
| AC-03 | 生成项目 manifest | 无 action path 以 `/gateway/` 开头 | 通过 |
| AC-04 | 13 条业务命令 | 完整路径均为 `<root>/gateway/cli/<service>/...` | 通过 |
| AC-05 | 用户修改隔离 | `.idea` 保持未触碰且未暂存 | 通过 |

## 风险、数据保护、回滚和上线观察

| 风险 | 控制 | 回滚/处置 |
| :--- | :--- | :--- |
| 路由调整影响已依赖错误地址的环境 | 完整 URL 测试锁定用户确认契约 | 以本任务差异做普通反向提交，不覆盖发布版本 |
| 生成器扩大无关差异 | 修改源 manifest 后运行生成器并逐项审查输出 | 停止并调查生成漂移，不覆盖用户修改 |
| 敏感信息进入证据 | 只使用 `example.com` 和路径统计，不发真实请求 | 停止扩散并安全移除 |

- **上线前验证**：本地 V3、发布包内容、下一补丁版本与 CI 应由后续发布任务完成。
- **上线后观察**：find/describe 与受影响业务命令的 HTTP 状态、网关鉴权和后端错误类型；不得记录 Cookie 或请求响应正文。
- **回滚入口**：普通反向提交本任务代码；已发布版本不可覆盖重发。

## 任务外发现

- `serve --project-apis` 当前用 `apiUrl` 作为上游基础地址，而正式业务命令用 `apiUrl/gateway/cli`；是否对齐属于独立行为变更。

## Git、规则与变更记录

- 已读取 R-01 至 R-10 中适用模块；使用 `diagnosing-bugs` 和 `mbs` 技能。
- 用户于 2026-08-14 明确要求“提交代码并发布1.0.3”；远端写入与 npm 发布统一在关联发布任务中审计。

| 时间 | 变更 | 原因 |
| :--- | :--- | :--- |
| 2026-08-14 | 创建 L3 修复需求、计划和冲突记录 | 用户授权修正已发布路由契约 |
| 2026-08-14 | 建立 3 条失败回归后修正召回常量和 13 条源 manifest 路径 | 证明并消除路由顺序及重复网关前缀根因 |
| 2026-08-14 | 完成生成、定向/全量测试、构建、15 个 URL 与差异核验 | 本地实现达到待验收状态，外部交付仍未授权 |
| 2026-08-14 | 用户授权提交并发布 1.0.3 | 将本修复纳入独立维护发布任务，不扩大 serve 或真实请求范围 |
