# 修正认证与召回地址契约执行计划

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 修正认证与召回地址契约 |
| 任务级别 | L3（认证入口、公共请求路由和已发布契约勘误） |
| 任务类型 | BUG |
| 任务状态 | 已完成 |
| 当前阶段 | 已随 1.0.2 发布并核验 |
| 交付状态 | 已发布 |
| 执行授权 | 已授权 |
| 授权来源 | 用户确认二维码/密码登录地址及 find 网关地址后明确要求“开始修改” |
| 授权范围 | 修改并验证当前 `1.0.0` 分支的配置根地址归一化、登录页和 find/describe 路由、相关测试与审计记忆；不含 `.idea`、版本升级、Git 提交推送、标签、全局安装或 npm 发布 |
| 计划完成后动作 | 完成本地 V3 验证后交付，等待后续提交或发布授权 |
| 验证级别 | V3 |
| 创建和更新时间 | 2026-08-12 |
| 需求文档 | [`./需求描述.md`](./需求描述.md) |
| 分支与基线 | `D:\workspace\mbs-cli` / `1.0.0` / `a14f7b0a53f6fd0184852258d8bed64a97623702` |
| 执行环境 | Windows / PowerShell / pnpm workspace |

## 恢复执行入口

- **任务目标**：让认证入口和语义召回严格使用用户确认的完整地址，并兼容历史 `/gateway` 配置。
- **当前进度**：配置归一、登录直连和召回网关路径修正已随 npm maintenance-1 `1.0.2` 发布；本地 V3、分支 CI、Release 与官方源包路由核验通过。
- **下一步**：无；后续地址变更需建立新任务并使用新补丁版本。
- **阻塞**：无。
- **待确认**：无；二维码、密码和 find/describe 完整路径均已由用户确认。
- **未验证**：真实生产登录和召回请求、发布包安装、远程 CI 与 npm 发布未执行。
- **重要警告**：`.idea/claudeCodeTabState.xml` 是用户修改，不得触碰；不得记录 Cookie 或登录 key。
- **执行授权**：已授权。
- **当前允许操作**：任务范围内文件修改与本地 V3 验证。
- **当前禁止操作**：修改凭据和生产数据、Git 暂存提交推送、版本、标签、全局安装和 npm 发布。
- **授权后的第一步**：已核对分支、基线、工作区、历史发布事实和现有地址拼接点。

## 已确认决策、冲突与约束

| 编号 | 决策 | 来源 | 状态 |
| :--- | :--- | :--- | :--- |
| D-001 | 登录页直接位于 API 根地址的 `/eshop/manager/...` | 用户确认 | 有效 |
| D-002 | find/describe 使用 `/cli/gateway/cli-service/cli/api/recall...` | 用户确认 | 有效 |
| D-003 | `apiUrl` 表示 API 根地址；兼容读取历史 `/gateway` 后缀但不改写文件 | 当前配置事实与最小兼容方案 | 有效 |
| D-004 | 其他业务路径规则保持不变 | 用户确认“其他正确” | 有效 |

### 规则冲突

| 编号 | 规则一 | 规则二 | 冲突内容 | 影响范围 | 当前处理 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| F-001 | 用户当前确认 | `20260812-[BUG]修复语义召回服务路由` | 用户确认包含 `/gateway`，旧发布审计写成不包含 | find/describe 与项目记忆 | 按用户当前决定建立部分修正任务，保留旧任务并追加勘误关系 | 已解决 |

- HTTP 请求继续只允许 GET 和查询类 POST。
- 不新增依赖，不改变 Cookie 缓存和刷新协议。
- npm `1.0.2` 已替代维护通道中的 `1.0.1`；`latest` 仍为 `0.1.58`。

## 影响范围

### 预计修改

- `packages/shared/src/config.ts`
- `packages/shared/src/__tests__/config.test.ts`
- `packages/cli/src/commands/login.ts`
- `packages/cli/src/__tests__/login.test.ts`
- `packages/cli/src/find/remote-client.ts`
- `packages/cli/src/__tests__/find.test.ts`
- `packages/cli/src/__tests__/remote-client-auth.test.ts`
- 当前任务审计索引与认证、统一语义发现记忆

### 明确不修改

- `.idea/claudeCodeTabState.xml` 和其他用户修改。
- Cookie/key 文件、全局安装和本机持久化配置文件。
- 普通业务接口、数据库相对路径、请求载荷和响应协议。
- 包版本、锁文件、Git 历史和远程状态。

## 执行前提

- 当前分支为 `1.0.0`，基线提交为 `a14f7b0`。
- 现有依赖已安装，不需要联网或新增依赖。
- 测试使用人工构造 URL，不访问真实服务，不记录凭据。

## 执行步骤

### S1：建立失败回归

- **状态**：已完成
- **目标**：让登录、召回和历史配置三类错误在当前代码上稳定失败。
- **验证**：运行 shared config 测试和 CLI login/find/recall-client 定向测试。
- **通过标准**：失败值准确对应多余 `/cli`、缺少 `/gateway` 或遗留 `/gateway` 配置。
- **证据**：修复前 4 个测试文件共 23 条测试中 8 条按预期失败，分别命中登录多余 `/cli`、召回缺少 `/gateway` 与历史配置未归一。

### S2：最小修复地址拼接

- **状态**：已完成
- **目标**：集中归一化 API 根地址，修复登录和召回专用前缀。
- **验证**：S1 的 23 条定向测试全部转绿；全量 shared、CLI 与业务包测试无回退。

### S3：V3 验证与记忆勘误

- **状态**：已完成
- **目标**：完成构建、全量回归、地址静态核对、敏感与工作区检查，并记录旧发布事实和本地待发布修正。
- **验证**：`pnpm build`、`pnpm test`、login/find/describe 帮助、`git diff --check`、完整 URL 脚本和精确差异检查均通过。

## 验收检查

| 编号 | 验收项 | 预期 | 状态 |
| :--- | :--- | :--- | :--- |
| AC-01 | 二维码登录 | `<root>/eshop/manager/login.jsp` | 通过 |
| AC-02 | 密码登录 | `<root>/eshop/manager/loginit2.jsp` | 通过 |
| AC-03 | 召回地址 | find/describe 包含 `/cli/gateway/cli-service` | 通过 |
| AC-04 | 历史配置兼容 | `<root>/gateway` 读取为 `<root>` | 通过，且读取未改写文件 |
| AC-05 | 无路径回退 | 认证刷新、普通业务和全量测试通过 | 通过 |
| AC-06 | 用户修改隔离 | `.idea` 不进入任务差异 | 通过，保持未触碰且未暂存 |

## 验证证据

| 检查 | 结果 |
| :--- | :--- |
| 修复前定向测试 | 4 个文件 / 23 条：8 条按预期失败，15 条通过 |
| 修复后定向测试 | 4 个文件 / 23 条全部通过 |
| `pnpm test` | 161 条全部通过（根脚本 12 + workspace 149） |
| `pnpm build` | 14 个 workspace 范围构建全部通过 |
| 帮助核验 | `login --help`、`find --help`、`describe --help` 全部退出码 0 |
| 本机有效 URL 静态核验 | 配置只读解析为 `http://www.instudio.me:6206`；四个确认地址逐字匹配 |
| 网络与敏感数据 | 未发送真实请求，未打印 Cookie、key、用户数据或响应正文 |

## 风险、数据保护、回滚和上线观察

| 风险 | 控制 | 回滚/处置 |
| :--- | :--- | :--- |
| 根地址归一化影响自定义子路径部署 | 只移除明确历史后缀 `/gateway`，保留其他路径 | 恢复本任务配置归一化差异 |
| 登录修复影响 key 捕获 | 保持监听页面和 `ERPLOGIN_PATH` 不变，仅改导航 URL | 恢复登录 URL 拼接并保留失败测试证据 |
| 召回再次遗漏网关层 | 断言 find 和 describe 的完整 URL | 恢复前缀或新增修复提交，不改写历史 |
| 敏感信息进入证据 | 测试只用 `example.com`，不输出 Cookie/key | 停止扩散并移除敏感内容 |

- **上线前验证**：V3、本地发布包内容检查和下一未占用版本核验需在未来发布任务中完成。
- **上线后观察**：登录页可达、Cookie 刷新成功、find/describe HTTP 状态与后端召回依赖健康；不得记录 Cookie 或查询正文。
- **回滚入口**：以本任务提交为单位普通反向提交；已发布版本不得覆盖重发。

## 任务外发现

- HTTP 403 自动刷新与错误包装可诊断性属于独立问题，本任务不修改。
- 动态 raw/export 绝对 URL 输入的 Cookie 外发风险需独立安全任务处理。

## Git、规则与变更记录

- 已读取 R-01 至 R-08、R-10；使用 `diagnosing-bugs` 和 `mbs` 技能。
- 当前仅允许本地修改与验证，不执行 Git 或 npm 外部写入。

| 时间 | 变更 | 原因 |
| :--- | :--- | :--- |
| 2026-08-12 | 创建 L3 修复需求、计划和冲突记录 | 用户授权修复认证和召回地址 |
| 2026-08-12 | 先建立 8 条失败断言，再完成最小代码修复 | 证明根因并避免错误契约再次固化 |
| 2026-08-12 | 完成定向、全量、构建、帮助和完整 URL V3 验证 | 本地交付完成，外部提交发布未授权 |
