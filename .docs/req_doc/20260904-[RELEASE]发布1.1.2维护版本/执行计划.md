# 执行计划：发布 1.1.2 维护版本

> 安全提示：发布证据只记录脱敏状态、版本、提交和作业标识，不得记录真实账号、密码、Cookie、Token、请求响应正文或签名地址。

## 任务元信息

| 字段 | 内容 |
| :--- | :--- |
| 任务名称 | 发布 1.1.2 维护版本 |
| 任务级别 | L3 |
| 任务类型 | RELEASE / SECURITY |
| 任务状态 | 已完成 |
| 当前阶段 | 已发布并完成官方源核验，归档 |
| 交付状态 | 已发布 |
| 执行授权 | 已授权 |
| 授权来源 | 用户明确要求“提交代码并发布npm版本1.1.2” |
| 授权范围 | 当前认证修复与关联审计、版本 1.1.2、普通推送 `origin/1.0.0`、维护标签、npm `maintenance-1` 发布及发布后核验和审计回填 |
| 计划完成后动作 | 持续执行至发布后官方源核验和审计回填完成 |
| 验证级别 | V3 |
| 创建/更新时间 | 2026-09-04 |
| 需求文档 | [需求描述.md](./需求描述.md) |
| 仓库与分支 | `D:\workspace\mbs-cli` / `1.0.0` / `origin/1.0.0` |
| 基线 | `168a05eb6669c4abf1fef804f912b05dac857a94` |
| 执行环境 | Windows / PowerShell / Node 20 兼容项目工具链 / pnpm 10.28.2 |

## 恢复执行入口

- **任务目标**：提交 compat-session 请求头修复并把 `@mb-it-org/cli@1.1.2` 发布到 npm `maintenance-1`。
- **当前进度**：功能提交 `50fa89c`、版本提交 `832423f`、两次分支 CI、维护标签、Release、npm dist-tag 与官方源隔离安装均已完成并通过。
- **下一步**：无；任务归档。真实认证联调仍需专用测试身份和独立授权。
- **阻塞**：无。
- **待确认**：无；沿用 1.x 维护分支契约，只移动 `maintenance-1`，保持 `latest` 不变。
- **未验证**：真实认证服务；未读取或发送任何真实凭据。
- **重要警告**：`pnpm release:patch` 只支持 `master/latest`，不得用于当前维护分支；版本提交和 `maintenance-1-v1.1.2` 必须手工按既有流程创建。
- **执行授权**：已授权。
- **当前允许操作**：任务范围内编辑、暂存和提交，普通推送维护分支，创建推送维护标签，监控 GitHub Actions 与 npm，发布后更新并提交审计。
- **当前禁止操作**：强推、改写历史、删除引用、修改 master/latest、覆盖旧 npm 版本、使用真实凭据请求。
- **授权方式**：当前用户消息已直接授权提交与发布 1.1.2。
- **授权后的第一步**：已核对仓库、分支、基线、工作区、远端、版本、标签、npm dist-tag 和现有任务文档。

## 已确认决策、假设与约束

| 编号 | 类型 | 内容 | 依据 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| D-001 | 决策 | 当前工作区认证修复、测试、Skill、长期规范、关联审计与记忆统一纳入功能提交 | 用户授权及工作区归属核对 | 有效 |
| D-002 | 决策 | 版本为 `1.1.2`，标签为 `maintenance-1-v1.1.2` | 用户指定版本与维护分支工作流 | 有效 |
| D-003 | 决策 | npm 只更新 `maintenance-1`，保持 `latest=0.1.59` | 1.x 长期维护分支契约 | 有效 |
| D-004 | 约束 | 仅普通推送，不改写、删除或覆盖历史和已发布版本 | R-08 与发布安全边界 | 有效 |
| D-005 | 约束 | 不使用或记录真实凭据 | R-06 与项目认证规范 | 有效 |

## 影响范围

- compat-session 请求头源码与测试。
- 关联修复任务、本机替换任务、审计索引、长期规范、Skill 和认证主题记忆。
- 本发布任务文档与 `packages/cli/package.json`。
- 发布后更新 1.x 维护分支记忆、认证记忆和关联任务交付状态。
- 明确不修改：master、latest、依赖、锁文件、真实认证缓存、生产配置和其他仓库。

## 执行前提与安全专项

- 受保护资产：npm 发布权限、GitHub 维护分支和标签、认证凭据与发布制品完整性。
- 不可信输入：远端分支状态、npm registry 查询、CI/Release 状态；发布前逐项校验，不根据单次缓存结果推断成功。
- 外部写入顺序：功能提交及分支 CI → 版本提交及分支 CI → 维护标签 → Release/npm → 官方包核验 → 审计回填。
- 失败行为：任一前置验证或 CI 失败即停止后续不可逆步骤；不重试掩盖失败，不强推。
- 凭据处理：发布命令不携带 npm Token；仅 GitHub Actions 使用仓库 Secret。
- 回滚：标签前使用普通修复提交；npm 发布后只允许更高版本前向修复。

## 已加载规则与技能

- R-01 至 R-10：已按当前 L3 发布和认证范围完整读取。
- `mbs`、`references/update/SKILL.md` 与 `references/global.md`：用于维护版本、Skill 和认证边界核对。
- `AGENTS.md`、`.docs/项目规范.md`、认证主题记忆、维护分支主题记忆、DEC-004、关联修复与上一维护发布任务：已完整读取。
- 并行评估：Git 推送、CI、标签与 npm 发布具有严格顺序；验证中互不写共享状态的检查可并行，发布步骤串行执行。当前上层协作约束不允许主动创建子代理。

## 执行步骤

| 步骤 | 状态 | 目标 | 前置条件 | 操作与修改对象 | 通过标准 | 失败处理 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| S-01 冻结范围与审查 | 已完成 | 确认所有待提交改动均属于当前修复和发布 | 用户授权、基线一致 | 差异、工作区、任务文档、远端与版本只读检查 | 全部文件可追溯；无未知修改、凭据或目标占用 | 停止提交并调查归属或冲突 |
| S-02 发布前 V3 | 已完成 | 验证依赖、测试、构建、发布守卫和 CLI | S-01 | frozen install、全仓测试构建、发布保护、CLI 冒烟、安全检查 | 项目全部既有检查通过，lockfile 无变化 | 分类失败并修复后完整复验 |
| S-03 功能提交与 CI | 已完成 | 提交并推送认证修复 | S-02 | 精确暂存当前修复与审计；普通推送 `origin/1.0.0` | 功能提交已推送且分支 CI 成功 | 不更新版本、不打标签；普通修复提交 |
| S-04 版本提交与 CI | 已完成 | 升级并提交 `1.1.2` | S-03 CI 成功 | `packages/cli/package.json`、发布任务状态 | 版本提交已推送且分支 CI 成功 | 不打标签；普通修复提交 |
| S-05 标签与 npm 发布 | 已完成 | 推送维护标签并等待 Release | S-04 CI 成功 | `maintenance-1-v1.1.2` | 标签精确指向版本提交；Release 成功；npm 精确版本可见 | 保留可审计状态，停止后续声明并调查 |
| S-06 官方源核验与回填 | 已完成 | 验证官方包、刷新 Skill 并同步发布事实 | S-05 | npm dist-tag、临时安装、MBS Skill、任务与记忆文档 | 官方包版本、帮助与 Skill 通过；审计事实完成回填 | 标记待验收或阻塞，不虚报发布完成 |

## 验收检查

| 编号 | 验收项 | 状态 |
| :--- | :--- | :--- |
| AC-01 | 全部改动审查与敏感信息检查 | 通过：当前差异均可追溯到关联修复、本机替换与本发布任务；未发现真实凭据或无关产物 |
| AC-02 | frozen install、全仓测试、全 workspace 构建 | 通过：lockfile 无变化，定向 26 项、全仓 259 项与 14 包构建通过 |
| AC-03 | 发布 workflow、release script/bundle 守卫与 CLI 冒烟 | 通过：workflow 守卫、7 项 script、5 项 bundle、版本/登录/刷新帮助及 Skill 冒烟通过 |
| AC-04 | 功能提交、版本提交和两次分支 CI | 通过：`50fa89c`、`832423f` 与 CI `33856724019`、`33856943465` 均成功 |
| AC-05 | 维护标签、Release、npm 精确版本与 dist-tag | 通过：标签指向 `832423f`，Release `33857086502` 成功，`maintenance-1=1.1.2` 且 `latest=0.1.59` |
| AC-06 | 官方包、Skill 刷新和审计回填 | 通过：官方源隔离安装的版本、login/refresh help、主 Skill、update Skill 与本机链接 Skill 均通过；审计事实已回填 |

## 风险与回滚

- 若差异出现真实凭据、未完成核心缺口或验证失败，停止提交和发布。
- 若远端分支前进，停止并调查；不强推、不自动覆盖。
- 若分支 CI 失败，不进入下一阶段；若 Release 失败，保留标签与作业入口并按原因处置。
- npm 发布成功后不可覆盖同版本；严重缺陷使用更高补丁版本。
- 发布后观察 GitHub Actions 结论、npm 精确版本、`maintenance-1` 和 `latest`、官方包版本及 Skill 可读性。

## 验证记录

- `pnpm install --frozen-lockfile`：15 个 workspace，锁文件无需更新。
- 认证定向：`session-login.test.ts` 17 项与 `index.test.ts` 9 项，共 26 项通过。
- 全仓测试：根脚本 13 项、shared 114 项、doris 21 项、export 3 项、legacy 10 项、CLI 98 项，共 259 项通过。
- 全仓构建：`pnpm build` 覆盖 14 个 workspace 包并通过，CLI Skill 复制成功。
- 发布保护：workflow 静态守卫通过；release script 7 项与 bundle 5 项通过。
- 离线冒烟：工作区 CLI 版本、login/refresh help、主 Skill 与 update Skill 读取成功。
- 请求头范围：编译后的 compat-session 无 `client-type` 赋值；普通业务 `http.js` 保留 `client-type: cli`。
- 分支交付：功能提交 `50fa89c` 与版本提交 `832423f` 已普通推送；CI `33856724019`、`33856943465` 均成功。
- 发布交付：`maintenance-1-v1.1.2` 精确指向 `832423f`；Release `33857086502` / 作业 `100972784390` 成功。
- 官方源：`@mb-it-org/cli@1.1.2` 可查询并完成隔离安装；`maintenance-1=1.1.2`，`latest=0.1.59`。
- 官方包验收：版本、login/refresh help、主 Skill 与 update Skill 均通过；本机链接 CLI 与 Skill 同步核验通过。
- 未执行：真实认证中心联调；未读取或发送真实凭据。

## 任务外发现

- 无。

## 变更记录

- 2026-09-04：用户授权提交当前修复并发布 `1.1.2`；确认目标版本和维护标签未占用，任务进入执行中。
- 2026-09-04：完成差异归属、远端、目标版本、标签与发布边界预检；未发现阻塞。
- 2026-09-04：frozen install、定向 26 项、全仓 259 项、14 包构建、发布守卫和离线 CLI/Skill 冒烟全部通过；进入功能提交阶段。
- 2026-09-04：功能提交 `50fa89c` 普通推送到 `origin/1.0.0`；分支 CI `33856724019` 成功，进入版本提交阶段。
- 2026-09-04：版本提交 `832423f` 推送后 CI `33856943465` 成功；维护标签 `maintenance-1-v1.1.2` 指向该提交。
- 2026-09-04：Release `33857086502` / 作业 `100972784390` 成功；npm 官方源传播完成后确认精确版本与 dist-tag。
- 2026-09-04：官方源隔离安装和本机链接 CLI/Skill 核验通过；审计事实完成回填，任务归档。
