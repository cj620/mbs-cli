# 接口语义发现

## 记忆元信息

- **记忆键**：`INTERFACE-SEMANTIC-DISCOVERY`
- **负责模块**：`packages/cli`、`skills`、`scripts/gen-from-manifest.mjs`
- **当前状态**：本地已验证/待发布
- **最后核验时间**：2026-08-12
- **最后核验基线**：`a14f7b0` 加当前工作区
- **权威来源**：`20260812-[FEATURE]新增公共只读请求命令`

## 当前事实

- API/workflow 发现只允许调用后端语义召回；后端不可用时明确失败，不读取本地 manifest 或接口 Markdown 产生候选。
- 完整接口定义只通过 `mbs describe <apiId>` 从后端按需读取；详情确认 `operationType=QUERY` 且 method 为 GET/POST 后，智能体按字段作用域构造 `mbs request`，不依赖预生成命令。
- `name` 与 `domain` 是有界、无控制字符的展示元数据，不自动等同于 audit manifest 命令身份。CLI 仅在二者都符合命令标识 allowlist 时派生可选 `command`；否则省略命令且禁止猜测或转写。
- `detailCommand` 只从正整数 API ID 派生，因此中文、camelCase 或带空格的合法展示名仍可安全进入 `describe`。
- 召回与详情响应经过白名单和结构上限校验；可选 diagnostics 只输出固定分类，不包含 URL、Cookie、响应体或异常正文。
- find 本地遥测不记录自然语言 query，只保留时间、remote 模式、候选数量和可选稳定 action。
- Skill 安装使用精确替换；生成业务域只保留 `SKILL.md`，不得恢复 `skills/manifest.json` 或生成接口端点文档。
- `mbs request` 复用共享 `/gateway/cli`、Cookie 与刷新逻辑；只接受单斜杠相对 path，拒绝绝对 URL、协议相对地址、穿越、查询串、片段和未替换占位符。

## 本地验证状态

- 当前工作区已验证 `find → describe → request` 的静态协议、16 条 request 契约、177 条全量测试、构建与命令发现；未发送真实业务请求，代码未提交、未推送或发布。
- 机器上的全局发布版 `mbs` 不包含当前分支命令；未在本轮覆盖全局 npm 安装。

## 演进关系

- 2026-07-23 之前的本地词法降级、`detailPath` 与接口 Markdown 方案已被后端唯一检索和后端详情替代。
- 保留业务域导航与 org/dashboard 等非接口型专用资料，不代表允许扫描本地目录发现业务接口。
- `20260806-[FEATURE]统一语义发现与数据库表召回` 在本协议上规划 table 候选与数据库只读路径；现有 find/describe 继续作为统一发现的 API 分支。
- `20260810-[BUG]修复语义发现展示名契约` 更正了“展示名必然是命令标识”的旧假设，不扩展后端公共响应。
- `20260812-[FEATURE]新增公共只读请求命令` 补充动态执行能力：可选业务 `command` 仅作便利入口，method/path/字段定义成为公共 request 的输入来源。

## 当前强制约束

- 不得恢复本地接口候选或将后端失败转换为成功结果。
- 不得把后端字符串直接当作 shell/CLI 命令执行。
- 只能在详情证明 QUERY、method 为 GET/POST 且必填参数已确认后调用 request；path 参数先替换，query/body 分开传递。
- 召回和详情请求只允许使用保存登录态与配置的标准 CLI 网关，不支持地址覆盖或命令行 Cookie 输入。
- 不得记录认证信息、响应正文、异常原文或用户自然语言查询。
- 发布、全局 CLI 替换和目标环境重建需要独立授权。

## 深入读取条件

涉及 find/describe 契约、Skill 生成或安装、远程失败诊断、遥测、目标环境联调、发布或回滚时，必须读取原始需求、执行计划和当前代码。

## 原始来源

- [`../../req_doc/20260812-[FEATURE]新增公共只读请求命令/需求描述.md`](../../req_doc/20260812-[FEATURE]新增公共只读请求命令/需求描述.md)
- [`../../req_doc/20260812-[FEATURE]新增公共只读请求命令/执行计划.md`](../../req_doc/20260812-[FEATURE]新增公共只读请求命令/执行计划.md)

- [`../../req_doc/20260722-[FEATURE]接口检索降噪与智能召回/需求描述.md`](../../req_doc/20260722-[FEATURE]接口检索降噪与智能召回/需求描述.md)
- [`../../req_doc/20260722-[FEATURE]接口检索降噪与智能召回/执行计划.md`](../../req_doc/20260722-[FEATURE]接口检索降噪与智能召回/执行计划.md)
- [`../../req_doc/20260810-[BUG]修复语义发现展示名契约/需求描述.md`](../../req_doc/20260810-[BUG]修复语义发现展示名契约/需求描述.md)
- [`../../req_doc/20260810-[BUG]修复语义发现展示名契约/执行计划.md`](../../req_doc/20260810-[BUG]修复语义发现展示名契约/执行计划.md)
